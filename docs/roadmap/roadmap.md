---
type: implementation-roadmap
authority: canonical
implementation: partial
verification: code-checked
source: "docs/roadmap/roadmap.md"
last_verified: "2026-08-27"
last_verified_commit: "working-tree"
owners: ["planning"]
---

# 구현 로드맵

이 문서는 승인된 설계를 어떤 순서로 구현할지, 각 작업의 상태와 완료 게이트가 무엇인지 관리한다.
시스템 동작과 계약의 정본은 각 Architecture·Security·Contracts 문서이며, 이 문서는 설계를
재서술하지 않는다. 상태와 ID 규칙은 [Roadmap 도메인 안내](README.md)를 따른다.

## 현재 구현 순서

각 단계는 이전 단계의 정상·실패 경로 검증을 통과한 뒤에만 활성화한다. 신규 Agent/Project 기능은
direct-worker Task 경로를 대체하지 않고 feature flag 뒤에서 점진적으로 도입한다.

| 단계 | 배포 가능한 increment | 포함 Roadmap ID | 우선순위 | 활성화 게이트 |
|---:|---|---|---|---|
| 0 | production trust foundation | #58, #59 | P1 | no-auth fail-closed, endpoint/tool authorization matrix, bootstrap 원문 DB 부재 |
| 1 | Worker identity와 credential authority | #60, #66, #55 | P1 | mTLS/self binding, Security Manager delivery grant, raw export 비활성화 |
| 2 | 실행 일관성과 제어 권한 | #62, #63, #61 | P1 | Task 상태 CAS, control epoch fencing, inventory-first recovery, bounded control channel |
| 3 | durable workspace와 공유 Host 안전성 | #69, #64 | P1 | Git checkpoint 복구, container isolation, privileged helper 거절 경로 |
| 4 | 최소 Project·Agent lifecycle | #48, #67, #49 | P2 | Project scope, one Agent/one Attempt, Hibernated E2E; WarmIdle 비활성 |
| 5 | 운영 완결성 | #68, #70, #65, #50 | P1~P2 | archive hold, reconciliation/alert, reproducible skill, attach security gate |
| 6 | 확장 기능 | #51, #52, #53, #54, #56 | P2 | 앞 단계 불변식 유지와 vendor/gateway 회귀 검증 |

**첫 착수 단위**는 #58의 `AuthorizationContext`/fail-closed middleware와 #59의 bootstrap digest
migration이다. 이 단계가 완료되기 전에는 Project/Agent control API, Security Manager delivery API,
MCP mutation 확장을 외부에 노출하지 않는다.

P1은 프로덕션 보안과 실행 신뢰성 게이트다. P2 기능 확장은 관련 P1 불변식을 우회하지 않는
범위에서만 병행한다. 긴급 credential 폐기·재발급 같은 사고 대응은 이 순서보다 우선한다.

## 활성 항목

### 기능 확장 (#48-#52)

| ID | 항목 | 상태 | 설계 정본 | 다음 완료 게이트 |
|---|---|---|---|---|
| #48 | Project 기능 | 설계 완료·구현 대기 (1·2·3단계 완료 2026-08-24) | [모델](../architecture/project-feature-design.md), [계약](../contracts/project-management.md) | **1단계(이번)**: `Project` 엔티티와 CRUD를 처음으로 실제로 만들었다 — 그동안 `ProjectId`/`tasks.project_id`는 존재했지만 참조 대상인 `projects` 테이블 자체가 없어 순수 미검증 메타데이터였다. `crates/fleet-core/src/project.rs`(`Project`, `ProjectStatus`), migration 022(`projects` 테이블, `tasks.project_id`에 FK는 걸지 않음 — 이 컬럼은 migration 이전부터 있어 실제 배포 DB에 이미 검증 안 된 값이 있을 가능성을 배제할 수 없어서), `Store::{create,get,get_by_name,list,update_status}_project`/`project_has_active_tasks`(PgStore+MemStore), Dashboard `GET/POST /api/projects`+`GET/DELETE /api/projects/{id}`, MCP `fleet_create_project`/`fleet_list_projects`/`fleet_delete_project`, `PermissionKind::{ProjectRead,ProjectCreate,ProjectDelete}`(Operator/Viewer 기본 역할에 `ProjectRead` 추가). `fleet_dispatch_task`의 `project_id`도 처음으로 실제 검증한다 — 존재하지 않거나 `active`가 아닌 project를 가리키면 제출을 거절한다(그동안은 저장만 되고 아무도 읽지 않는 순수 메타데이터였다). **의도적으로 축소한 것**: `ProjectStatus`는 목표 5-상태가 아니라 `Active`/`Draining`/`Archived` 3-상태다 — `Draft`/`ArchiveBlocked`가 검증할 하부 구조(AgentTemplate, Agent process/lease/credential grant cleanup)가 이 저장소에 없어서, 지금 만들면 항상 도달 불가능한 죽은 상태가 된다(`#70`이 죽은 `FailureKind` variant를 제거한 것과 같은 판단). archive(`DELETE`/`fleet_delete_project`) 게이트도 목표 계약의 전체 절차가 아니라 "이 project를 참조하는 비종료 Task가 없다" 하나뿐이며, 그 게이트를 통과하면 `request_id`나 `202`+비동기 progress 없이 같은 요청 안에서 곧바로 `Draining → Archived`까지 진행한다(idempotent 재호출은 안전 — 같은 게이트를 다시 평가할 뿐). `PATCH`(정책 변경)와 host/worker 배정 endpoint, `fleet_update_project_policy`는 문서가 이미 명시한 차단 조건(보안 모델 승인 미완료) 그대로 손대지 않았다. Agent admission·`max_active_agents`/`max_warm_agents` 강제·Worker eligibility selector는 여전히 없다 — dispatch는 project_id 검증을 통과한 뒤에도 project 무관 일반 풀 규칙 그대로 실행된다. 신규 테스트: `crates/fleet-store/tests/projects.rs` 8건(실제 Postgres), `crates/fleet-dashboard/tests/dashboard_api.rs`에 project API 11건(권한·CSRF·중복 이름·404·archive 게이트·idempotent 재호출 포함), `crates/fleet-mcp/src/handlers.rs`에 project tool 7건(round-trip, 빈 이름/중복 이름 거절, 미존재 project 삭제, active task가 있으면 draining 유지, dispatch_task의 존재하지 않는/archived project 거절 2건 포함), `crates/fleet-core/src/project.rs`에 단위 테스트 4건. 검증: `cargo check --workspace --all-features`, `cargo test --workspace`(98 스위트 전체 `ok`), `DATABASE_URL`+`FLEET_MCP_CAPABILITIES` 주입 `fleet-store`/`fleet-api`/`fleet-scheduler`/`fleet-mcp`/`fleet-dashboard` 재실행(전체 통과 — 단, 이 작업과 무관한 사전 존재 버그 하나를 우연히 발견해 별도 태스크로 분리함: MCP가 미등록 tool 이름에 JSON-RPC `-32601` 대신 `-32600`을 반환하는 문제, clean main에서도 동일하게 재현 확인), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0 **2단계(이번)**: 두 표면의 비대칭을 닫고 규칙을 단일화했다. (1) Dashboard `POST /api/tasks`가 `project_id`를 받는다 — 1단계에서는 MCP만 받을 수 있어 계약 문서가 요구하는 "Dashboard와 MCP의 동일한 권한·오류 응답"이 성립하지 않았다. (2) 검증·archive 규칙을 `fleet_store::project_rules`(`ensure_project_accepts_new_tasks` / `advance_project_archive`)로 추출해 두 표면이 공유한다 — 1단계에서 archive 절차를 Dashboard와 MCP에 복제했던 것도 이 공유 구현으로 되돌렸다(검증 로직까지 세 번째로 복제하기 전에 멈춘 것). (3) 이어가기(`parent_task_id`)가 부모의 Project를 상속한다(`Task::inherit_from_parent`) — 그러지 않으면 한 thread가 절반은 Project 안, 절반은 일반 풀에 걸쳐 경계의 의미가 사라진다. 상속된 값도 명시 입력과 똑같이 검증하므로 부모의 Project가 그 사이 닫혔으면 이어가기도 거절된다. (4) `TaskSummary`에 `project_id`를 노출하고, New Task 폼에 Project 드롭다운을 추가했다(`api/projects`에서 `active`만 채운다 — 어차피 서버가 거절할 선택지를 보여주지 않는다; `project:read` 권한이 없으면 403을 받아 "None"만 남는다). **실제 브라우저 검증**: `tail -f /dev/null | fleet serve`로 stdin EOF 종료를 우회해(이 세션에서 `#75`/`#76`/`#63` 때 반복해 막혔던 제약의 해법) 실제 Postgres + 대시보드를 띄우고, bootstrap→로그인→New Task 페이지까지 브라우저로 진행해 드롭다운이 active 2건만 알파벳순으로 표시하고 archived 1건을 제외함을 확인, 폼으로 실제 제출해 task가 올바른 `project_id`로 DB에 저장되고 `/api/tasks`가 그 값을 돌려주는 것까지 확인했다. archived/미존재 project 제출은 400과 명확한 메시지로 거절되며 task 행이 생성되지 않음도 실측했다. 신규 테스트: `project_rules.rs` 6건, `fleet-core::task` 3건(상속), `dashboard_api.rs` 6건(project_id 기록·미존재/archived/malformed 거절·상속·닫힌 Project로의 이어가기 거절). **3단계(이번)**: Dashboard 화면 — `/projects`(목록), `/projects/new`(생성), `/projects/:id`(상세). 1·2단계로 API를 두 겹 쌓았지만 대시보드에서는 아무것도 보이지 않는 상태였다. **구현 중 문서 모순을 발견해 함께 정정했다**: `ui-design.md` §3.9(2026-08-14)가 목록에 "Host/Worker 배정 수"(`list_project_hosts`/`list_project_worker_ids`)를 요구하는데, 모델 정본 `project-feature-design.md`의 공유 실행 풀 불변식은 "Host와 Worker에는 `project_id`를 두지 않는다"고 못 박는다 — UI 문서가 그 결정보다 앞서 작성돼 갱신되지 않은 것이다. 명세를 그대로 구현하면 정본과 충돌하므로 화면은 모델을 따르고 UI 문서의 해당 행을 폐기 표시했다. 명세가 요구하는 `agent_provisioning_mode`/`workdir_template`/`default_agent_template_id`/`agent_idle_timeout_secs`도 1단계가 의도적으로 제외한 정책 필드라 폼에 넣지 않았고, 그 사유를 UI 문서 "구현 상태" 절에 남겼다. 상세 화면은 그 Project의 Issue 목록(`#88`/`#92`)과 Task 목록을 함께 보여주며, archive는 비종료 Task가 남으면 `draining`에 머물고 버튼이 "Retry archive"로 바뀐다. 권한은 페이지 수준에서 막는다 — `project:create`가 없으면 생성 폼 자체가 403이다(폼을 채우게 한 뒤 제출에서 거절하지 않는다). 사이드바는 12개 HTML에 손으로 복제돼 있고 동기화 자동화가 없어(마커만 있고 읽는 코드가 없다) 스크립트로 일괄 추가한 뒤 **모든 페이지에 Projects 링크가 있는지 강제하는 테스트**를 넣었다. **실제 브라우저 검증**: 실제 Postgres + 대시보드를 띄워 bootstrap→로그인 후 목록 렌더링, 상세의 Issue/Task 표시, archive가 pending task 때문에 draining에 머물며 그 사유를 화면에 표시하는 것, task를 종결시킨 뒤 재시도해 archived로 넘어가고 버튼이 사라지는 것, 생성 폼으로 실제 Project가 DB에 저장되는 것(`created_by`가 세션 사용자로 기록됨)까지 확인했다. 신규 테스트 4건(권한별 페이지 200/403, 사이드바 링크 일괄 확인) **4단계(2026-08-27, 보안 모델 승인)**: 차단 조건 1(`ProjectPolicyManage`와 `AgentCreate`의 관계)이 사람 승인으로 닫혔다. 승인 시점까지 제안 문언 자체가 없었으므로 규칙을 새로 썼고, 정본은 `docs/security/authorization-and-audit.md`의 "Project 정책 변경과 Agent 생성의 관계"다 — Agent 수·provisioning 대상을 바꾸는 정책 필드는 `project:policy_manage`에 더해 `agent:manage`를 요구하는 **필드별 게이팅**(`#86`의 H1 선례와 같은 형태), 권한 확인 시점은 Task 제출이 아니라 **정책 쓰기**(반대로 하면 모든 contributor가 `agent:manage`를 가져야 해 Project 정책이 의미를 잃는다), 메타데이터 편집은 Agent를 만들지 않으므로 무관. 함께 세 건의 노후 문언을 정정했다: (a) 차단 문구가 가리킨 `project:assign`·`agent:create`는 목표 capability 표에 **없는 이름**이었다(대응은 `agent:manage`), (b) 함께 묶여 있던 host·worker Project 배정 endpoint는 공유 실행 풀 불변식이 이미 배제해 승인 대기가 아니라 설계상 부재, (c) `PATCH /api/projects/{id}`는 메타데이터 편집이라 보안 차단 대상이 아니며 남은 선행 조건은 동시 편집 의미(revision 또는 `If-Match`, `request_id`) 확정이다. **차단 조건 2·3은 여전히 열려 있다** — 사람 결정이 아니라 테스트 조건이고 대상인 Agent 엔티티와 `worker_execution_leases`(`#67`)가 없어 작성할 수조차 없다. 따라서 `fleet_update_project_policy`와 정책 컬럼은 그대로 차단이며, `project:policy_manage`/`agent:manage` capability도 만들지 않았다(관리·검사할 대상이 없어 죽은 권한이 된다). 집행 증명은 `authorization-and-audit.md` 구현 게이트 9로 등록했다. 코드 변경은 노후 사유를 적고 있던 주석 2건뿐(`fleet-core/src/auth.rs`, `fleet-dashboard/src/handlers.rs`) |
| #49 | Agent provisioning·memory·summary·tool binding | 1단계 완료(2026-08-28)·2단계 대기 | [Agent provisioning](../architecture/agents/provisioning.md), [외부 계약](../contracts/agent-management.md) | **1단계(이번)**: Agent 엔티티와 그 판독자 3종만 만들었다. `crates/fleet-core/src/agent.rs`(`Agent`, `AgentStatus`, `AgentFilter`), `AgentId`, migration 027(`agents` 테이블 — `project_id`는 `projects(id)`를 참조하는 **실제 FK**에 `ON DELETE RESTRICT`, 이름은 `UNIQUE (project_id, name)`으로 Project 안에서만 유일), `Store::{create,get,get_by_name,list,update_status}_agent`/`project_has_live_agents`(PgStore+MemStore), MCP `fleet_create_agent`/`fleet_list_agents`/`fleet_stop_agent`, Dashboard `GET|POST /api/agents`+`DELETE /api/agents/{id}`, Project 상세 페이지의 Agents 절(목록·인라인 생성 폼·Stop), `PermissionKind::{AgentRead,AgentManage}`(`AgentRead`는 Operator·Viewer 기본, `AgentManage`는 Admin 전용), 감사 이벤트 `agent.create`/`agent.stop`. **FK 판단이 `#48`과 반대인 이유**: `022`가 `tasks.project_id`에 FK를 걸지 않은 것은 그 컬럼이 migration 이전부터 있어 검증 안 된 값이 배포 DB에 있을 수 있어서였다. `agents.project_id`는 이 migration이 **만드는** 컬럼이라 선행 데이터가 존재할 수 없으므로 같은 기준이 정반대 결론을 낸다. **Agent 행이 죽은 데이터가 아님을 보장하는 판독자 3종**(이것이 이 단계의 핵심): (1) 생성 시 `ensure_project_accepts_new_agents`가 Project 상태를 검사해 archived/draining Project에는 Agent를 만들지 못하게 한다, (2) `advance_project_archive`가 살아 있는 Agent를 archive 게이트로 세어 archive가 이제 "비종료 Task 없음 **그리고** 살아 있는 Agent 없음" 두 조건이 된다, (3) `agent:read`/`agent:manage`가 드디어 검사할 대상을 갖는다. `022_projects.sql`이 남긴 "순수 미검증 메타데이터" 함정을 같은 커밋에서 판독자를 함께 랜딩시켜 피한 것이다. **의도적으로 축소한 것**: `AgentStatus`는 목표 8-상태가 아니라 `Ready`/`Stopped` 2-상태다 — `Starting`/`Running`/`Failed`/`Draining`은 상태를 옮길 주체인 Worker 제어 스트림(`#89`)이 없고, `WarmIdle`은 execution lease(`#67` 후속)가, `Hibernated`는 snapshot 불일치 판정에 필요한 AgentTemplate(`#86`)·harness 구성(`#51`)이 없다. 9-필드 명령 봉투·ACK·`generation`/`control_epoch`/`fencing_token`·재조정·`OutcomeUnknown`도 전부 `#89` 선행이다. `tasks.agent_id`는 지금 만들면 항상 NULL인 컬럼이 되므로(dispatch가 Agent를 고르지 않는다) 만들지 않았다. `agent:attach`도 만들지 않았다 — 붙을 터미널 세션(`#50`)도 step-up grant 발급자도 없다. `project_id`는 불변이라 Store 갱신 메서드·API 필드·PATCH route를 아예 만들지 않았다(옮기려면 대상 Project에 새 Agent를 만든다). 회수는 멱등이며 이미 `stopped`인 Agent를 다시 회수하면 쓰기를 건너뛰고 **같은 `updated_at`**을 반환한다 — 테스트가 실제 버그를 잡았다: 처음 구현은 store 기록 후 로컬 구조체 필드만 고쳐 첫 응답이 기록 **이전** 타임스탬프를 실었고, 두 번째 호출이 다른 값을 돌려줬다. 저장된 행을 다시 읽어 고쳤다(MCP·Dashboard 양쪽). **함께 정리한 문서 드리프트**: `authorization-and-audit.md`가 "Agent 엔티티가 랜딩할 때 정리한다"고 예약해 둔 `AgentCreate`/`agent:create`/`AgentDelete` 표기를 `agent:manage`로 통일했다(`ui-design.md`, `agent-template.md`, `issues.md`, provisioning sequence 다이어그램). `docs/reviews/*`와 `docs/log.md`는 그 시점 근거 보존이 목적이라 손대지 않았다. `dashboard-api.md`의 route 표에 `#48`/`#92`가 랜딩할 때 누락된 `/api/projects`·`/api/issues`·`/api/task-threads` 행도 함께 채웠다(사전 존재 드리프트). **여전히 열려 있는 것**: `#48`의 차단 조건 2·3은 Agent 엔티티가 생겼음에도 그대로다 — 2번이 시험하는 *자동* provisioning 경로가 없고(Task 제출은 Agent를 만들지 않는다) 3번의 lease가 없다. `authorization-and-audit.md` 게이트 9도 `projects`에 정책 컬럼이 하나도 없어 여전히 시험 불가다. 신규 테스트: `crates/fleet-store/tests/agents.rs` 10건(실제 Postgres — FK와 `(project_id, name)` 유일성이 SQL 계층에서만 강제되므로 `StoreError::Conflict` 번역은 실 DB로만 증명된다), `dashboard_api.rs` Agent API 9건(권한 403 2종·CSRF·중복 409·미존재/archived Project 400·멱등 회수·404), `fleet-mcp` 도구 7건, `project_rules.rs` 4건, `fleet-core::agent` 4건. archive 게이트 테스트는 세 계층 모두 **Task를 하나도 만들지 않는다** — 그래야 Project를 `draining`에 붙잡는 것이 Agent 행뿐임이 증명된다. `tests/agents.rs`는 저장소의 다른 통합 테스트 14개가 쓰는 "매 테스트 TRUNCATE" 관례를 **따르지 않는다** — 초안이 그 관례를 베꼈다가 병렬 실행에서 10건 중 6건이 FK 위반으로 실패했고, 그 관례는 `ci.yml`이 붙이는 `--test-threads=1`에 의존한다(실측: `projects.rs` 병렬 3~4건 실패·직렬 8건 통과). 대신 테스트마다 유일한 이름의 Project를 만들어 공유 상태 자체를 없앴다 — 특정 스레드 수에서만 통과한다는 전제를 파일 밖 CI 설정에 숨기지 않기 위해서다. 나머지 14개 파일은 `#49`의 범위가 아니라 손대지 않았다(자세한 실측은 [log](../log.md) 2026-08-28 항목). **2단계 이후**: #48/#67 뒤 Hibernated 단일 Agent E2E; WarmIdle은 별도 flag |
| #50 | Agent terminal monitoring·CLI attach | 설계 완료·구현 대기 | [Terminal access](../architecture/agents/terminal-access.md) | #49 Phase 4 뒤 cleanup·attach 권한·gateway E2E 검증 |
| #51 | Agent harness·Skill·project constitution | 설계 완료·구현 대기 | [Harness composition](../architecture/agents/harness-composition.md) | #49 tool binding과 합성 순서·권한·revision 검증 |
| #52 | Multi-vendor Agent runtime | 설계 완료·구현 대기 | [Runtime adapters](../architecture/agents/runtime-adapters.md) | #49 Phase 0에서 transport와 native Skill 동작 검증 |

### LLM gateway (#53-#56)

| ID | 항목 | 우선순위·상태 | 정본 | 완료 게이트 |
|---|---|---|---|---|
| #53 | Worker LLM proxy 설정 원자성 | 완료 | [LLM Gateway](../architecture/llm-gateway.md) | `crates/fleet-worker/src/config.rs`의 `WorkerConfig::validate()`가 `[llm_proxy]`의 `gateway_url`/`api_key` 중 한쪽만 채워진 조합과 스킴 없는 `gateway_url`을 `WorkerError::Config`로 fail-closed 거부. `from_str` 통합 테스트 `llm_proxy_gateway_url_only_is_rejected`, `llm_proxy_api_key_only_is_rejected`, `llm_proxy_gateway_url_without_scheme_is_rejected`, `llm_proxy_valid_combination_parses_ok`, `llm_proxy_section_absent_parses_ok`로 검증 |
| #54 | liteLLM 배포 hardening | **완료 (2026-08-23)** | [배포 Runbook](../deployment/litellm-gateway.md) | 저장소가 직접 증명하는 유일한 자산인 `docker-compose.yml`의 `litellm` service와 `examples/litellm-config.yaml`을 네 항목 전부 고쳤다. **① 고정 image version**: floating `main-latest` → 실제 발급된 stable release `ghcr.io/berriai/litellm:v1.98.0`(GitHub releases·GHCR 태그 목록 둘 다로 실재 확인). **② secret 외부 주입**: `LITELLM_MASTER_KEY`가 compose 파일에 원문(`sk-litellm-master-key`)으로 박혀 있어 셸에서 같은 이름의 env var를 설정해도 무시됐다 — `${LITELLM_MASTER_KEY:-sk-litellm-master-key-change-me}`로 바꿔 외부 주입이 실제로 먹히게 했고, `examples/litellm-config.yaml`의 `general_settings.master_key`도 하드코딩 대신 이미 provider key들이 쓰던 `os.environ/...` 관례를 따라 `os.environ/LITELLM_MASTER_KEY`로 바꿔 값을 한 곳(container env var)에서만 관리하게 했다. **③ healthcheck**: `postgres`/`orchestrator`엔 있었는데 `litellm`엔 없었다 — 이미지 실제 Dockerfile을 확인해 Chainguard wolfi-base 기반이라 `curl`/`wget`이 없다는 걸 먼저 확인하고(흔한 `curl`/`wget` 기반 healthcheck를 그대로 썼으면 이미지에 없는 바이너리를 호출해 항상 unhealthy로 빠졌을 것), 이미지에 이미 있는 `python3`(stdlib `urllib`)로 인증 불필요·DB 연결까지 확인하는 `/health/readiness`(liteLLM 공식 문서로 경로·인증 여부 확인)를 호출하도록 작성했다. `orchestrator`의 `depends_on.litellm.condition`도 `service_started`(컨테이너만 뜨면 통과)에서 `service_healthy`(healthcheck 통과까지 대기)로 바꿔, 오케스트레이터 첫 dispatch가 gateway 준비 전 connection-refused로 실패할 여지를 없앴다. **④ 비공개 기본 bind**: `4000:4000`(모든 호스트 인터페이스에 publish)을 `127.0.0.1:4000:4000`으로 바꿨다 — master key가 새면 등록된 모든 LLM 공급자 API 키를 대신 소비당할 수 있어 orchestrator API 노출보다 위험도가 높다고 판단. **검증 한계 명시**: 이 환경엔 docker daemon이 없어(`docker`/`docker compose` 미설치) 실제 컨테이너 기동으로 healthcheck·bind·depends_on 순서를 동적으로 재현하지 못했다 — `python3 -c "import yaml..."`로 두 YAML 파일의 문법 오류만 정적 확인했고, image 태그·health 엔드포인트 경로·base 이미지의 바이너리 구성은 각각 GitHub releases/GHCR 태그 목록/공식 문서/실제 Dockerfile을 직접 조회해 교차 확인했다(이 저장소 코드로 실행해 확인한 게 아니라는 뜻 — `#82`~`#85`가 실제 SSH/TLS로 검증한 것과는 확증 등급이 다르다). 다음에 docker가 있는 환경에서 `docker compose up -d litellm`으로 실제 healthcheck 통과·bind 확인·연쇄 기동 순서를 재검증하는 게 권장된다 |
| #55 | Worker-scoped gateway credential | P1 · 설계 필요 | [LLM Gateway](../architecture/llm-gateway.md), [보안 모델](../security/control-plane-security-model.md) | principal·scope·발급·전달·회수·회전·감사 계약과 격리 테스트 |
| #56 | Provider·gateway·quota 실패 상태 분리 | P2 · 설계 필요 | [LLM Gateway](../architecture/llm-gateway.md), [Routing](../architecture/tasks/routing-policy.md) | 상태·telemetry·fallback 계약과 오분류 방지 테스트 |

### 보안과 실행 신뢰성 (#57-#76)

| ID | 항목 | 우선순위·상태 | 정본 | 완료 게이트 |
|---|---|---|---|---|
| #57 | Bootstrap token 공개 식별자 전환 | P1 · 완료 | [보안 발견 S9](../security/reports/security-findings.md) | API·CLI(`token_id` 발급/list/revoke) 라운드트립과 원문 미노출을 `crates/fleet-api/tests/bootstrap_tokens.rs`로, 017 마이그레이션 이전 발급된 plaintext 토큰이 마이그레이션 뒤에도 원문으로 인증됨을 `crates/fleet-store/tests/bootstrap_token_migration.rs::legacy_plaintext_token_still_authenticates_after_digest_migration`으로 검증 |
| #58 | HTTP·MCP principal/capability authorization | P1 · 부분 구현 | [보안 모델](../security/control-plane-security-model.md) | HTTP scoped bearer·MCP launcher capability allow-list에 이어, CF Access 전용 배포에서도 `auth_middleware`가 검증된 `VerifiedUser.email`을 `AuthorizationContext.principal_id`로 연결하고 `authorize_http_endpoint` capability 검사를 반드시 거치도록 고쳤다(이전엔 CF-only 경로가 capability 검사 자체를 건너뛰었음). ~~매핑 없는 CF principal은 호환성을 위해 잠정적으로 전체 capability를 받는다~~ — 이 잔여 gap은 `#74`(완료, 2026-08-23)가 fail-closed로 닫았다. Project scope/confused-deputy 중 **리소스 간 경계**(다른 Project 소속 Task를 `issue:link`로 끌어와 존재·label을 노출하는 경로)는 2026-08-25 닫았다(`crates/fleet-dashboard/src/handlers.rs::link_issue_task_api`). **principal 단위 Project scope(호출자가 자기 Project만 보는 것)는 여전히 미착수** — `fleet-core::auth`에 User를 특정 Project로 한정하는 멤버십 개념 자체가 없어서, 지금 "principal이 이 Project에 속하는가" 검사를 추가하면 비교 대상이 없어 항상 통과하는 죽은 분기가 된다. 멤버십 모델 자체의 설계 승인이 선행돼야 하며, `project-management.md`가 관련 배정 endpoint를 `project:assign`/`agent:create` 관계 확정 전까지 의도적으로 미노출로 묶어 둔 것과 같은 이유로 보류한다 |
| #59 | Bootstrap token digest 저장 | P1 · 완료 | [보안 모델](../security/control-plane-security-model.md) | digest 저장·1회 원문 반환·digest 기반 회수에 이어, 실제 PostgreSQL에 001~018 마이그레이션을 전부 적용하고 발급된 bootstrap token·join으로 얻은 worker operational token 모두 `pg_dump` 결과에 원문이 나타나지 않음을 `crates/fleet-api/tests/bootstrap_token_dump.rs::issued_bootstrap_token_never_appears_in_a_database_dump`로 검증 |
| #60 | Worker operational identity | P1 · 부분 구현 | [보안 모델](../security/control-plane-security-model.md), [Enrollment](../contracts/worker-enrollment.md) | join은 digest-only operational token을 1회 발급하며, bootstrap 소비·Worker 생성·credential 저장이 atomic enrollment transaction으로 묶였다(PgStore `.begin()`, MemStore 단일 lock). register/heartbeat/deregister는 `worker:self` binding을 검사해 다른 worker_id를 조작하면 403을 반환한다(테스트로 검증됨). credential rotate/revoke API(`worker:credential:manage` capability), stdin/token-file bootstrap 전달, worker.toml legacy `bootstrap_token` fail-closed 거부까지 구현·테스트 완료(6~8단계). mTLS(9단계), staging rehearsal(10단계)은 PKI/staging 인프라 부재로 착수 전. 상세는 [Worker credential 전환](worker-credential-migration.md) |
| #61 | Worker liveness mode | P1 · 부분 구현 | [Worker liveness](../architecture/worker-liveness-policy.md) | 설계 문서의 5단계 중 1~2단계 완료: `WorkerLivenessMode`(`periodic`\|`on_demand`, 기본값 `periodic`) 필드가 스키마·register API·worker.toml 전 구간에 배선됐고, HealthChecker는 `on_demand` Worker를 heartbeat-timeout 기반 Offline 판정에서 제외한다. **3~5단계(on_demand 실제 dispatch용 bounded ACP probe, `last_activity_at` 기록, 1000-idle-worker E2E)는 의도적으로 미착수** — 설계 문서 자신이 명시하듯 heartbeat 없이 Agent start/stop/capture 명령을 전달할 별도 control-stream/bounded poll 인프라(`#67` 영역, 미구현) 없이는 on_demand를 실제로 안전하게 켤 수 없다 |
| #62 | 실행 일관성·멱등성 | P1 · 부분 구현 (1단계 완료 2026-08-25, 2·3·4단계 완료 2026-08-26) | [실행 일관성](../architecture/tasks/execution-consistency.md) | **1단계(이번): 상태 전이 compare-and-set.** 정본의 검증 게이트 10개 중 앞의 두 개("Failed 이후 늦은 Completed 이벤트가 거부된다", "취소와 완료 경쟁에서 하나의 터미널 상태만 선택된다")를 닫았다. `fleet-core`에 `TransitionOutcome{Applied, Rejected{current}}`를, `Store`에 `compare_and_set_task_status(id, expected: &[TaskPhase], new)`를 추가했다. PgStore는 `UPDATE tasks SET status = $2 WHERE id = $1 AND status_phase = ANY($3)` 단일 statement로 구현한다 — `status_phase`는 001_init.sql이 이미 만들어 둔 생성 칼럼(`status->>'phase'`)이고 002가 인덱스를 걸어 두었으므로 **신규 마이그레이션이 없다**. MemStore는 같은 lock 안에서 read-compare-write. 거절은 `Err`이 아니라 `Ok(Rejected)`다 — 다른 writer가 먼저 상태를 옮긴 것은 장애가 아니라 정상적인 관측 결과이기 때문이다. 무조건 덮어쓰던 `update_task_status` 호출 6곳(`fleet-cli` 1 + `Dispatcher` 5)을 전부 걷어냈고, 그 자리에 CAS 호출 지점 5곳(`fleet-cli` 1 + `Dispatcher` 4 — dispatch 직전, `Completed` 워커 이벤트, 공용 `mark_failed`, `Dispatcher::cancel`)이 들어갔다. 호출 지점 수가 전이 경로 수보다 적은 것은 실패 마킹이 전부 공용 `Dispatcher::mark_failed`를 지나며 `expected`만 달리 넘기기 때문이다 — 그 경로는 워커 `Failed` 이벤트와 transport 실패(`Dispatched`), dispatch 준비 실패와 reconciler dead-letter(`Pending`), reconciler orphan·offline(`Dispatched`)이다. 취소 두 경로(`Dispatcher::cancel`, `fleet tasks cancel`)는 `Pending`\|`Dispatched`를 받는다. `expected`는 공용 `allowed_predecessors` 기본값이 아니라 **호출 지점별로 좁게** 넘긴다 — `mark_failed`가 두 선행 집합에 걸쳐 있어서 합집합을 하드코딩하면 orphan sweep이 방금 `Pending → Dispatched`로 옮겨간 작업을 죽여 닫으려던 경쟁을 그대로 남긴다. 전이가 거절되면 `append_event`를 발행하지 않는다(상태가 안 바뀌었는데 `task_completed`를 남기면 이벤트 로그가 일어나지 않은 일을 주장한다). dispatch 경로는 transport로 보내기 **전에** 중단하므로(`DispatchError::NotPending`) 아무도 소유하지 않는 실행이 생기지 않는다 — 부수적으로 `dispatch_ready_tasks`의 이중 dispatch 창도 닫힌다. 검증: `crates/fleet-store/tests/task_cas.rs` 9건을 MemStore·실제 PostgreSQL 양쪽에서(`both_backends!`) 실행. `a_late_completion_cannot_overwrite_a_reconciled_failure`가 정본 게이트 1, `only_one_of_two_racing_cancels_applies`·`a_completion_cannot_land_on_a_cancelled_task`가 게이트 2에 대응한다. 거절 시 저장된 상태가 **바뀌지 않았음**까지 확인하므로 "Rejected를 반환하면서 실제로는 썼다"는 구현은 통과하지 못한다. **2단계(2026-08-26): 클라이언트 멱등성 — 정본 게이트 3.** `TaskRequest.idempotency_key`와 길이 접두 SHA-256 페이로드 해시(`PAYLOAD_HASH_VERSION = "fleet.task.payload.v1"`), 마이그레이션 024의 `(created_by, idempotency_key)` **부분** 유니크 인덱스(`WHERE idempotency_key IS NOT NULL` — Postgres는 유니크 인덱스에서 NULL을 서로 다르게 취급하므로 MemStore도 이를 명시적으로 흉내낸다), `Store::insert_task_idempotent`가 `IdempotentInsert{Inserted, Duplicate(Task), Conflict{existing_task_id}}`를 반환한다. 1단계의 `TransitionOutcome`과 같은 이유로 거절이 `Err`이 아니라 `Ok` 값이다 — 재제출은 장애가 아니라 정상적인 클라이언트 동작이다. `Dispatcher::submit`은 `Duplicate`에서 **즉시 반환**한다: TaskCreated 이벤트도, 의존성 검사도, 워커 선택도, dispatch도, RUNNING 게이지 증가도 실행되지 않는다. 소비 표면은 MCP `fleet_dispatch_task`와 대시보드 `POST /api/tasks`이며 둘 다 응답에 `deduplicated`를 싣는다 — 이 플래그가 없으면 이미 `Completed`인 중복이 `dispatched: false` + `warning` 없음으로 나와 대시보드 문서가 정의한 "재시도 예약됨"과 구분되지 않는다. `fleet-cli`는 의도적으로 비멱등이다(로컬 CLI에는 재시도하는 클라이언트가 없다). 검증: `fleet-store/tests/task_idempotency.rs` 6건을 MemStore·실제 PostgreSQL 양쪽에서, `Dispatcher::submit` 2건, 대시보드 HTTP 왕복 3건(200/같은 id/`deduplicated`, 409, 빈 문자열 키). 디스패처 2건은 **뮤테이션으로 값을 증명했다** — 조기 반환을 지우면 실패하고, 조기 반환을 `append_event` 뒤로 옮겨도 이벤트 수 단언이 잡는다(store 테스트 6건은 그 변형에서 전부 초록으로 남는다). **3단계(2026-08-26): control epoch fencing — 정본 게이트 4의 쓰기 절반.** 착수 시 범위는 `TaskAttempt` 계층 전체였으나 **실행 가능한 부분이 게이트 4의 쓰기 절반뿐임을 확인하고 축소했다** — 근거는 아래와 정본의 [구현 상태와 유예](../architecture/tasks/execution-consistency.md) 표. `fleet-store`에 `ControlFence{cluster_id, epoch}`를, `fleet-core`에 `TransitionOutcome::Fenced`를 추가하고 `compare_and_set_task_status`에 `fence: Option<&ControlFence>` 인자를 얹었다. PgStore는 `EXISTS (SELECT 1 FROM control_plane_lease WHERE cluster_id = $4 AND epoch = $5)`를 **기존 UPDATE와 같은 문장 안에** 넣는다 — 별도 SELECT로 먼저 확인하면 확인과 쓰기 사이가 그대로 TOCTOU 창이 되어 1단계가 없앤 것을 되살린다. `Fenced`를 `Rejected`와 나눈 이유는 **후속 동작이 다르기** 때문이다: `Rejected`는 다른 writer가 이 Task 하나를 먼저 옮긴 것이라 그 Task만 포기하면 되지만, `Fenced`는 이 인스턴스가 더 이상 제어 기관이 아니라는 뜻이라 이후의 모든 쓰기도 같은 이유로 실패한다. `LeaseObserver`가 `fence()`로 현재 관측을 술어로 변환하고 `FleetState::control_fence()`가 이를 dispatcher의 네 CAS 지점(dispatch, `Completed` 이벤트, `mark_failed`, `cancel`)에 배선한다. `fleet tasks cancel`은 의도적으로 `None`을 넘긴다 — `fleet serve`가 죽었거나 fenced된 상황에서도 동작해야 하는 operator 도구라 lease를 획득하지 않으며, 그 대가로 이 경로만 epoch 술어를 우회한다(정본에 기록). **`#63` 2단계의 bool 게이트와 겹치지 않는다**: 그쪽은 이미 `Fenced`를 관측한 뒤를 막고, 이쪽은 관측이 아직 `Active`인데 저장소만 앞서 간 창(갱신 주기 5초 안의 장애 전환)을 막는다. 검증: `fleet-store/tests/task_cas.rs`에 4건 추가(MemStore·실제 PostgreSQL 양쪽) — 게이트 4 대응 테스트는 lease를 재획득해 `fresh.epoch > stale.epoch`를 단언한 뒤 위상이 완벽히 일치하는 Task에 낡은 fence로 CAS해 `Fenced`를 받고 저장된 위상이 안 바뀌었음까지 확인하므로, 거절 원인이 epoch 하나임이 고정된다. `dispatcher.rs`에 배선 테스트 1건 — 저장소만 epoch 2로 옮긴 뒤 observer는 `Active{epoch:1}`로 두어 `lease_allows_control()`이 **통과하는** 상태를 만들고 `CancelError::ControlPlaneFenced`를 확인한다. **뮤테이션으로 값을 증명했다**: 그 호출 지점의 `fence` 인자를 `None`으로 바꾸면 이 테스트만 정확히 실패한다. **검증 한계**: 실제 두 프로세스의 라이브 장애 전환 중 쓰기 거절은 관찰하지 않았다(`#63` 3단계의 라이브 검증도 정상 종료 경로만 덮었다). READ COMMITTED 잔여 창(단일 문장 실행 **중** 커밋되는 epoch는 보이지 않음, 마이크로초 규모)은 `FOR SHARE` 없이 남겨 두고 정본에 기록했다 — 닫으려면 모든 Task 상태 쓰기가 5초 주기 lease 갱신과 같은 한 행에서 락 경쟁을 하게 된다. **4단계(2026-08-26): `TaskAttempt`를 만들지 않기로 결정하고, 그 안에서 유일하게 새로웠던 사실만 컬럼 하나로 기록했다.** 사용자 결정으로 **실행 재시도를 하지 않는다** — 실패한 Task는 터미널로 남고 다시 하려면 새 Task를 만든다. 그 결과 Task와 시도는 **1:0..1**이 된다. 실측으로 확인했다: `TaskStatus::Pending`을 **새 상태로 쓰는 코드가 저장소 전체에 없다**(`expected` 슬라이스 밖의 모든 출현은 테스트 `matches!` 단언이거나 읽기 측 표시·메트릭 match arm — `fleet-api/src/metrics.rs:111`, `fleet-cli/src/runtime.rs:1436`·`1613`). Task는 `Pending`으로 태어나고 다시는 돌아오지 않으므로 `Pending→Dispatched` CAS는 Task당 영구히 최대 한 번 성공한다. 그러면 `task_attempts`의 `worker_id`는 `TaskStatus::Dispatched{worker_id}`의, `created_at`은 `tasks.dispatched_at`의 중복이고 `generation`은 항상 1, `AttemptId`는 소비자가 없다 — **새로운 것은 `control_epoch` 하나뿐이다.** 그것만 남긴 이유: `control_plane_lease`는 **현재** epoch만 들고 있어 lease가 넘어가면 "이 Task를 어느 세대가 디스패치했는가"가 사후에 복원 불가능해진다. 나머지 셋은 오늘 `tasks`에서 읽힌다. 그래서 마이그레이션 026이 `tasks.dispatch_control_epoch BIGINT CHECK (>= 0)` 한 컬럼을 더하고, 3단계가 만든 CAS **안에서** `is_dispatching && fence.is_some()`일 때만 쓴다. 두 조건의 **교집합**인 이유는 두 겹이다 — Postgres 자리번호는 텍스트의 최댓값으로 개수를 추론하므로 `$5`가 fence 없이 문장에만 남으면 문장이 거절되고, `is_dispatching` 조건을 떼면 완료·실패 전이가 값을 덮어써 "디스패치한 세대"가 "마지막으로 손댄 세대"로 바뀐다. API 변경은 0이다(`Task` 구조체 필드 하나, `#[serde(skip_serializing_if)]`). 검증: `task_cas.rs`에 3건 추가(MemStore·실제 PostgreSQL 양쪽) — epoch 기록, fence 없을 때 미기록, **그리고 E1에서 디스패치 → lease 해제 → `instance-b`가 E2로 재획득 → E2에서 완료 후에도 값이 `Some(E1)`으로 남는지**. 셋째 테스트만이 `is_dispatching` 가드를 지킨다(뮤테이션으로 확인). 부수 수정: `migration_lease_guard.rs`가 관측 지점을 마이그레이션 025의 인덱스 이름으로 하드코딩하고 있어 **어떤** 새 마이그레이션이든 깨뜨렸다 — `_sqlx_migrations` 원장 조회로 일반화했다(sqlx는 DDL과 원장 삽입을 한 트랜잭션에 넣으므로 원장 부재는 스키마 객체 부재와 동등한 증거이고 마이그레이션에 무관하다). **검증 한계**: 이 컬럼을 **읽는 코드는 없다**(후보 소비자는 reconciler orphan sweep). HA lease가 없는 단일 인스턴스 배포에서는 `SchedulerState::control_fence()`가 `None`이라 **항상 NULL**이다 — "값을 못 구했다"가 아니라 "제어 세대 개념이 없는 배포"라는 뜻이다. 026 이전 행은 소급 불가다. 라이브 2프로세스 장애 전환은 이번에도 관찰하지 않았다. **정책이 만든 새 구멍(미해결)**: 외부 멱등성 키가 **Project ID + Task ID + effect scope + 제출 시 정책 revision**의 HMAC이라, 실패를 새 Task로 옮기면 키가 달라져 redrive가 같은 부작용을 두 번 적용할 수 있다. 새 Task 경계를 넘는 앵커 필드가 없으므로 만들지 않고 정본 유예 표에 **설계 미결**로 올렸다. **남은 범위**: effect ledger와 그것을 채울 생산자, 게이트 4의 이벤트 절반(오늘 **어떤 이벤트도 생산자 epoch를 싣지 않는다** — 싣지 않는 값은 비교할 수 없다), 비가역 tool effect의 자동 재호출 금지 — 정본 게이트 5~10. **이 항목에 포함하지 않은 것**: 다른 도메인 문서 25개(reviews 제외)에 남은 `TaskAttempt` 목표 설계 서술 — 실측 30개 파일 161행(이 원장 파일 제외)이며 개명이 아니라 교차 도메인 판정이라 `#97`로 분리했다. 근거 전문은 [재시도 정책 결정 기록](../reviews/task-retry-policy-decision-2026-08-26.md), 미룬 이유는 정본의 [구현 상태와 유예](../architecture/tasks/execution-consistency.md) 표와 [작업 로그](../log.md)에 남겼다 |
| #63 | Cold Standby fencing | P1 · 부분 구현 (1·2단계 완료 2026-08-23~24, 3·4단계 완료 2026-08-26) | [권한과 장애 전환](../architecture/control-plane-authority-and-failover.md) | **1단계**: control plane lease/epoch primitive. `fleet-store`에 `control_plane_lease` 테이블(migration 021)과 `Store::{acquire,renew,release,get}_control_lease`를 추가했다 — `epoch`는 획득마다 단조 증가하고, `acquire`는 `INSERT ... ON CONFLICT DO UPDATE ... WHERE expires_at < NOW()` 단일 원자적 statement로 "만료된 lease만 가로챈다"를 구현, `renew`는 `(instance_id, epoch, expires_at > NOW())` 3중 CAS라 이미 만료된 lease를 instance_id·epoch만 맞다고 "갱신"이라는 이름으로 되살릴 수 없다. `fleet-scheduler::lease::LeaseManager`가 그 위에 획득→갱신 루프(HealthChecker와 동일한 `new`/`spawn`/`status` 패턴)와 `LeaseStatus::{Stopped,Active,Fenced}` 관측 상태를 얹었다. **2단계(이번)**: 이 lease를 실제 배선했다. `FleetState.lease: Option<LeaseObserver>`(`with_lease` builder, 미설정 시 항상 허용 — 기존 단일 인스턴스 배포와 호환) + `lease_allows_control()`을 추가하고, `Dispatcher::dispatch_existing`(워커 선택·breaker 변경·transport dispatch 전부 포함하는 단일 choke point) / `Dispatcher::cancel` / `Dispatcher::handle_worker_event`(`Completed`/`Failed`만 — `Output`은 권위 있는 결정이 아닌 순수 관측 데이터라 fenced 상태에서도 계속 흘려보낸다) / `Reconciler::reconcile_once`(개별 시도가 아니라 sweep 전체를 스킵 — "Reconciler는 Active epoch에서만 동작한다") 네 곳에서 거절한다. `fleet-cli::run_serve`가 이제 모든 배포(단일 인스턴스 포함)에서 무조건 `LeaseManager`를 spawn한다 — 경쟁자 없는 획득은 사실상 즉시 성공해 실질 지연이 없고, 이 메커니즘이 실제 배포 어디서도 실행되지 않는 죽은 코드로 남는 걸 피한다. `cluster_id`는 이 저장소가 아직 단일 클러스터만 지원하므로 `"default"` 고정값. **아직 안 한 것**: Worker 안의 `worker_execution_lease`/fencing token(로드맵 `#67`), epoch를 읽어 쓰기를 거르는 강제(불변식 4·5 / 구현 게이트 3 = `#62` 검증 게이트 4) — 바인딩 대상인 fencing token이 `#67` 범위라 착수하지 않았다(**2026-08-26 정정**: 이 중 Task 상태 쓰기 쪽은 `#62` 3단계가 `ControlFence` 술어로 닫았다 — 단 **Dispatcher를 지나는 네 경로에 한한다**. `fleet tasks cancel`은 Dispatcher를 거치지 않고 저장소를 직접 CAS하므로 여전히 펜싱되지 않으며, 이는 의도된 유예다(운영자 CLI는 lease를 잃은 인스턴스에서도 동작해야 한다). MCP `fleet_cancel_task`는 `Dispatcher::cancel`을 타므로 펜싱된다. 여전히 남은 것은 Worker 쪽 fencing token과 늦은 **이벤트**의 거부이며, 후자는 `TaskAttempt` 선행이다). 신규 테스트: `dispatcher.rs`에 6건(fenced일 때 submit/cancel/Completed/Failed 거절, Output은 계속 처리됨을 명시적으로 확인하는 대칭 테스트 포함), `reconcile.rs`에 1건(sweep 전체가 스킵되고 `ReconcileSummary::default()`와 정확히 일치함을 확인). **3단계(이번, 2026-08-26)**: graceful shutdown 시 lease 즉시 반납 + 라이브 failover 검증 + Runbook. `LeaseManagerConfig.shutdown_grace`(기본 5초)와 `CancellationToken` 기반 `LeaseManagerHandle::shutdown()`을 추가했다. 반납을 핸들이 직접 하지 않고 **루프 안에서** 하는 이유: `release`는 현재 Active epoch로 CAS하는데, 핸들에서 부르면 갱신 루프와 같은 상태를 두 태스크가 읽어 읽은 직후 재획득으로 epoch가 바뀌면 무효한 epoch로 반납하게 된다 — 루프 안에서 하면 epoch 소유자가 유일해 경쟁 자체가 성립하지 않는다. 종료 신호는 **대기 지점**(`interval.tick()`, `sleep`)에서만 관측하고 `try_acquire`/`try_renew` 주변에서는 보지 않는다 — DB future를 중간에 버리면 커밋됐는지 모르는 lease가 남는다. `abort()`는 손대지 않았다(비정상 종료를 흉내내는 용도로 계속 쓰인다). 두 의미가 뭉개지지 않도록 대칭 테스트 2건으로 잠갔다: `graceful_shutdown_releases_the_lease_so_a_standby_acquires_at_once`, `abort_keeps_the_lease_held_so_a_standby_must_wait_for_the_ttl`. `fleet-cli::run_serve`에는 `SIGTERM`/`SIGINT` 핸들러(`wait_for_shutdown_signal`)를 새로 넣었다 — **그전까지 `shutdown()`은 도달 불가능한 코드였다**(stdin EOF 경로만 있었다). 신호 경로 끝에 `std::process::exit(0)`을 둔다: `run_mcp_server`의 `tokio::io::stdin()`은 전용 blocking 스레드에서 읽으므로 `select!`가 future를 버려도 진행 중인 `read(2)`는 취소되지 않고, stdin이 닫히지 않는 배포(systemd 파이프, 터미널)에서는 런타임 종료가 그 스레드를 기다려 **lease는 반납했는데 프로세스만 남는다** — 실제로 라이브 검증에서 실측해 발견했다. `LeaseManagerHandle::shutdown()`의 grace 초과 분기도 고쳤다: `timeout(grace, handle)`이 만료되면 `JoinHandle`이 drop되는데 tokio에서 그것은 취소가 아니라 detach라, 종료 중인 프로세스의 갱신 루프가 계속 살아 "반납도 TTL 만료도 없는" 상태가 된다 — `abort_handle()`을 미리 잡아 명시적으로 abort한다. 문서: 정본에 [구현 상태와 유예](../architecture/control-plane-authority-and-failover.md) 절을 신설해 게이트 7개의 상태와 두 인수인계 경로(정상 반납 = 명시적 증거 / TTL 만료 = 시계뿐, 불변식 3 위반)를 표로 남겼고, [운영 Runbook](../deployment/operations.md)의 수동 승격 절차에 "TTL 만료는 승격의 근거가 아니다"와 `expires_at - last_renewed_at`으로 종료 유형을 판별하는 SQL을 넣었다 — 단, 그 gap은 **소유자가 아직 죽은 Primary이고 lease가 만료돼 있을 때만** 의미가 있다. 살아서 갱신 중인 lease도 gap이 항상 TTL이고, 획득이 그 행을 통째로 덮어쓰므로 승격 뒤에는 전 소유자의 증거가 남지 않는다(warm standby에서 그 창은 `poll_interval` 수준). 이 전제를 빠뜨린 첫 초안을 리뷰에서 잡아 고쳤다. **라이브 검증(실제 Postgres, `fleet serve` 프로세스 2개)**: A가 epoch=1 획득 → B는 기동해도 획득 실패(구현 게이트 1) → A에 `SIGTERM` → **1.7ms 만에 lease 반납 후 프로세스 실제 종료** → B가 **1.93초 만에 epoch=2 획득**, 그 시점 TTL이 13.85초 남아 있어 승격이 TTL 만료로는 설명되지 않는다. **검증 한계**: `fleet-cli::run_serve`의 실제 배선 자체(LeaseManager spawn → FleetState 주입)는 컴파일·코드 리뷰로만 확인했다 — 2단계까지는 `fleet serve`의 MCP stdio 특성(`#75`/`#76` 검증 때도 마주친 문제) 때문에 라이브 관찰을 하지 못했고(3단계에서 `tail -f /dev/null`로 stdin을 열어 두어 해소했다), 각 레이어(Store CAS는 실제 Postgres 동시성, Dispatcher/Reconciler 거절 로직은 수동으로 구성한 fenced observer)를 개별적으로 실제/현실적인 조건에서 검증했다. 검증: `cargo check --workspace --all-features`, `cargo test --workspace`(97 스위트 전체 `ok`), `DATABASE_URL` 재실행(31 스위트 통과), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0. **3단계의 검증 한계**: 라이브 실행이 관찰한 것은 **정상 종료 경로뿐**이다 — TTL 만료 인수인계, 수동 승격 절차, Worker 재연결, reconciliation(구현 게이트 4)은 어느 것도 실행하지 않았다. Standby는 이미 기동해 polling 중인 **warm** 상태였으므로 1.93초는 `poll_interval`(3초) 안의 수치이며, 계약이 기술하는 Cold Standby(운영자가 승격 시점에 기동)의 수치가 아니다. `fleet-cli`의 신호 처리 경로(`SIGTERM` → 정리 → `exit(0)`)를 덮는 **테스트가 없다** — 이 경로를 통째로 지워도 모든 게이트가 초록으로 남는다(단위 테스트는 프로세스를 새로 띄우지 않아 원리적으로 잡지 못한다). `shutdown_grace` 초과 분기도 DB 무응답을 재현할 하네스가 없어 테스트가 없다 **4단계(2026-08-26): 기동 호환성 게이트 — 구현 게이트 5의 schema 절반.** 정본이 게이트 5의 근거로 적어 둔 "기동 시 호환성 검사 자체가 없다"가 **절반은 사실이 아님을 코드로 확인하고** 나머지 절반을 닫았다. sqlx 0.8.6은 `ignore_missing` 기본값이 `false`이고 이 저장소가 `set_ignore_missing`을 부르지 않으므로, **DB에 적용됐는데 바이너리에 없는** 마이그레이션은 이미 `VersionMissing`으로 거절된다 — "DB보다 낡은 바이너리"는 예전부터 기동하지 못했고, 없던 것은 메커니즘이 아니라 그 보장을 고정하는 **테스트**였다. 반대로 **바이너리에만 있는** 마이그레이션은 `Migrator::run_direct`의 `None => conn.apply(...)` 가지에서 말없이 적용되며 이쪽이 실제 구멍이었다: Cold Standby는 Primary와 DB 하나를 공유하므로, 더 새 바이너리를 든 Standby는 **기동하는 것만으로** 살아 있는 Primary 밑에서 스키마를 바꾼다(sqlx의 advisory lock은 경쟁하는 마이그레이터끼리만 직렬화하므로 이 상황을 덮지 못한다 — 위험한 쪽은 마이그레이션하지 않고 **이미 돌고 있는** 낡은 프로세스다). `PgStore::migrate`에 `guard_migration_against_live_lease`를 넣어 **적용할 마이그레이션이 있고 동시에 만료되지 않은 lease가 있을 때만** 거절한다. 두 조건을 모두 요구하는 것이 설계의 핵심이다 — 적용할 게 없어도 막으면 평범한 롤링 재기동이 통째로 막히는 운영 함정이 되고, `release_control_lease`가 행을 지우는 대신 `expires_at = NOW()`로 만들기 때문에 **정상 종료한 Primary는 TTL을 기다리지 않고 즉시 길을 비킨다**(사람을 막는 창은 크래시 뒤 최대 TTL 15초뿐이며, 에러 메시지가 남은 초·멈춰야 할 인스턴스·클러스터 이름을 모두 적는다). 게이트를 `Store::migrate` **구현 안**에 둔 덕에 `fleet serve`·`fleet migrate`·`fleet users`·`fleet doctor` 네 호출 지점이 구조적으로 전부 덮인다. **binary 버전 검사는 의도적으로 넣지 않았다** — 바이너리 버전을 DB에 쓰는 생산자가 저장소에 없고 `control_plane_lease`에 버전 컬럼도 없어, 지금 술어를 넣으면 항상 참인 죽은 검사가 된다. 그래서 게이트 5는 **닫힘이 아니라 부분**이다. 신규 테스트 `crates/fleet-store/tests/migration_lease_guard.rs` 4건은 `Migrator`의 필드가 공개돼 있다는 점을 이용해 **버전을 잘라낸 사본**을 조립함으로써, 체크섬을 위조하지 않고 진짜 `_sqlx_migrations` 원장과 함께 "DB는 N-1까지, 바이너리는 N까지"인 임시 DB를 만든다. **뮤테이션으로 값을 증명했다**: `migrate()`에서 가드 호출 한 줄을 지우면 4건 중 정확히 2건이 실패하고 나머지 2건(sqlx 기존 보장 고정 / 과잉 발동 방지)은 통과한 채 남는다 — 이 2:2 분할 자체가 각 테스트가 서로 다른 것을 보고 있다는 증거다. **라이브 검증**: 임시 DB를 마지막 마이그레이션 직전까지 올리고 `expires_at = NOW() + 120s`인 lease 행을 넣은 상태에서 실제 바이너리의 `fleet migrate`와 `fleet serve`가 **둘 다 exit code 1로 거절**되고 `_sqlx_migrations` 최대 버전이 불변임을, lease를 `expires_at = NOW()`로 반납한 직후 `fleet migrate`가 즉시 성공함을 확인했다. **함께 정정한 것**: 정본 게이트 표의 게이트 3 행도 낡아 있었다 — `516f85e`(`#62` 3단계)로 쓰기 절반이 닫혔는데 "미착수"로 남아 있었고, 근거로 든 `crates/fleet-api/src/state.rs`는 **존재하지 않는 파일**이다. 부분으로 고치고 epoch 절 본문도 함께 갱신했다. **4단계의 검증 한계**: 검사와 실제 적용 사이는 원자적이지 않다 — 검사 직후 다른 인스턴스가 lease를 얻으면 스키마는 여전히 바뀔 수 있다. 이 게이트는 배포 실수를 막는 것이지 분산 합의가 아니며, 원자적으로 만들려면 마이그레이션을 lease 아래에 넣어야 하는데 `control_plane_lease`를 **만드는 것이** 021 마이그레이션이라 부트스트랩 순환이 생긴다. 라이브 검증은 lease 행을 직접 INSERT해 "살아 있는 Primary"를 흉내 냈을 뿐 두 번째 `fleet` 프로세스를 실제로 띄우지는 않았고, `fleet users`/`fleet doctor` 두 경로는 같은 함수를 호출한다는 사실로만 덮여 있다. Primary가 크래시하고 TTL이 지난 뒤에는 이 게이트도 막지 못한다 — DB만 보고 살아 있는 Primary와 죽은 Primary를 구분할 수 없다는 불변식 3의 한계와 같은 뿌리다. |
| #64 | Agent 실행 격리 | P1 · 설계 확정·구현 대기 | [실행 격리](../architecture/agents/execution-isolation.md) | 다른 Agent의 process·workspace·credential 접근 차단과 wipe 실패 안전성 테스트 |
| #65 | 재현 가능한 Skill loading | P2 · 설계 확정·구현 대기 | [Harness composition](../architecture/agents/harness-composition.md) | revision 불일치 fail-closed, manifest 기록, 재실행 입력 동일성 테스트 |
| #66 | Worker LLM credential 보관·전달 강화 | P1 · 부분 구현 | [보안 모델](../security/control-plane-security-model.md), [Credential 레지스트리](../credentials/registry.md) | 이번에 닫힌 범위(인가·감사): `crates/fleet-api/src/app.rs`의 `required_capability` 행렬에 LLM credential 하위 자원(`/workers/{name}/credentials…`) 네 route가 전부 빠져 있어, 인증만 통과하면(capability가 빈 principal이나 워커 자신의 `fwo_` operational 토큰으로도) 어떤 워커의 LLM 프로바이더 API 키든 `GET .../export`로 평문 열람이 가능했고 감사 기록도 남지 않았다(`DELETE .../credentials/{model}`은 `worker:delete` 행렬 항목에 잘못 흡수돼 워커 토큰으로도 삭제 가능). `PermissionKind`에 `worker:llm_credential:read`/`:export`/`:manage` 세 capability를 신설하고(operational identity용 기존 `worker:credential:manage`와 이름·의미 모두 분리), GET 목록=read, GET export=export, PUT/DELETE=manage로 fail-closed 매핑했다. export는 저장·삭제 권한으로도 열리지 않는다 — 프로비저너 토큰(read+export)이 credential을 덮어쓰거나 지울 수 없게 하기 위해서다. `export`/`put`/`delete`는 `AuditEvent`(`worker.llm_credential.*`)로 기록하며, export는 감사 기록에 실패하면 평문을 반환하지 않는다(누가 키를 가져갔는지 남기지 못하면 열람 자체를 거부한다). **운영 영향**: 기존 배포의 프로비저너/CLI bearer 토큰 manifest에 새 capability를 추가하지 않으면 `fleet provision`의 `PushCredentials` 스텝이 403으로 실패한다. 남은 범위: TaskAttempt·Worker에 바인딩된 1회용 전달 grant(`#62` 의존, 미착수), 암호화 backend 추상화 교체, rotation 전체 워크플로, Security Manager 분리 | `crates/fleet-api/tests/worker_llm_credential_authz.rs`(capability 없는 principal·워커 자신의 operational 토큰이 export/list/delete에서 403이고 응답에 키가 실리지 않음, export 성공 시 감사 이벤트 1건 기록·비밀 값 미포함, `manage`만으로는 export 불가, `worker:delete`로는 credential 삭제 불가), `crates/fleet-api/src/app.rs`의 `capability_matrix_covers_llm_credential_routes`/`llm_credential_routes_do_not_collide_with_operational_credential`(단수형 `/credential`과 복수형 `/credentials` 분리), `crates/fleet-core/src/auth.rs`의 `llm_credential_permissions_are_distinct_from_operational_credential` |
| #67 | Worker execution lease·Agent command ACK | P1 · 부분 구현 (1단계 완료 2026-08-27) | [권한과 장애 전환](../architecture/control-plane-authority-and-failover.md), [Agent provisioning](../architecture/agents/provisioning.md) | **1단계(이번): dispatch 세대 술어 — 불변식 ②를 Task 단위에서 닫았다.** 한 프로세스가 리스를 잃었다 되찾는 창(epoch 5로 디스패치 → 6이 다른 곳에서 재디스패치 가능 → 같은 프로세스가 7로 재획득)에서, epoch 5의 늦은 결과는 위상도 `Dispatched`로 맞고 `516f85e`가 넣은 lease 술어도 **성립하므로**(지금 제어 기관이 맞다) 그대로 통과했다. `#62` 4단계가 만든 `tasks.dispatch_control_epoch`(migration 026) 위에 `AND (dispatch_control_epoch IS NULL OR dispatch_control_epoch = $5)`를 더해 닫았다. **술어의 발동 조건은 목표 상태가 아니라 전이의 출처다** — 새 `TransitionOrigin{WorkerOutcome, ControlDecision}`을 호출자가 선언하고 `WorkerOutcome`에만 건다. `Completed`/`Failed`로 유도하려던 최초 설계는 무너졌다: `Failed`는 워커 보고이기도 하지만 reconciler가 고아를 회수하며 **현재 보유자가 지금 내리는 결정**이기도 해서, 거기까지 걸면 epoch 5에 디스패치된 고아를 epoch 7 보유자가 영원히 회수 못 하는 라이브락이 된다. 6개 `mark_failed` 호출 중 `WorkerEvent::Failed` 핸들러 하나만 `WorkerOutcome`이다. 거절은 `TransitionOutcome::StaleDispatchEpoch{dispatched_under}`로 따로 보고한다 — `Fenced`에 접으면 "이후 모든 쓰기도 실패"라는 반대 신호가 되고, `Rejected{current}`에 접으면 위상이 실제로 맞았는데 `current: Dispatched`를 보고해 없는 경합 조사로 보낸다. NULL은 통과시킨다(026이 규정한 "제어 세대 개념이 없는 배포"). **의도적으로 만들지 않은 것**: `worker_execution_lease`의 CAS slot claim·`fencing_token`(Agent 엔티티 부재), `worker_incarnation`·Agent command ACK·self-fencing(워커→오케스트레이터 제어 스트림 자체가 없다 → `#89`), `agent_id`를 실은 dispatch(→ `#49`), WarmIdle NULL-`task_id` lease 행(생산자 없음), `OutcomeUnknown`(설계 미정). **남은 창**: 같은 epoch 안의 재디스패치는 세대가 아니라 시도(attempt) 단위 신원이 필요해 구분 불가 — 지금은 `#62` 4단계의 무재시도 정책이 그 창을 막고 있고, **재시도 정책이 바뀌면 다시 열린다.** 신규 테스트 `crates/fleet-store/tests/task_cas.rs` 5건(두 백엔드 모두). **검증 한계**: 전부 단일 프로세스가 순차로 리스를 뺏고 되찾는 재구성이며, 두 오케스트레이터가 동시에 뜬 라이브 실행은 하지 않았다 — 창의 존재는 코드 대조로만 확인했다. **다음 게이트**: `#49`의 Agent 엔티티 뒤 `worker_execution_lease` CAS slot claim·fencing token, `#89`의 제어 스트림 뒤 Worker incarnation·ACK 유실 `OutcomeUnknown`·self-fencing E2E |
| #68 | Project archive·retention·hold | P2 · 설계 확정·구현 대기 | [Lifecycle](../architecture/project-task-agent-lifecycle.md) | idempotent drain, effect/cleanup hold, retention/reopen, archive-blocked E2E |
| #69 | Project Git workspace·checkpoint recovery | P1 · 설계 확정·구현 대기 | [Entity placement](../architecture/entity-placement-and-context.md), [실행 격리](../architecture/agents/execution-isolation.md) | Agent worktree isolation, checkpoint push/restore, secret scan, Worker 이동 복구 E2E |
| #70 | Observability·reconciliation·recovery | P1 · 설계 확정·구현 대기 (부분 착수) | [관측성·재조정·장애 복구](../architecture/observability-and-reconciliation.md) | 본체(inventory-first recovery, orphan/grant/effect quarantine, alert/runbook E2E)는 Reconciler·`worker_execution_lease`·effect ledger 같은 아직 없는 하부 구조(`#63`·`#67` 선행)에 의존해 착수 전이다. **`#90` 취소로 흡수된 좁은 조각만 완료(2026-08-23)**: `/metrics`가 `fleet_tasks_total{phase="failed"}`로 실패 총량만 알려주고 원인을 분해하지 않아 "credential 미프로비저닝으로 몇 건 실패했나"를 집계할 수 없던 gap을, 신설 `fleet_tasks_failed_total{kind}` gauge로 닫았다(관측되지 않은 kind도 0으로 명시 — 라인 부재와 0건을 구분). 함께 정리하기로 했던 `FailureKind`의 `Timeout`·`AuthFailed`·`Cancelled` 세 variant는 실제 생성 지점을 조사한 결과 전부 제거가 맞다고 판단해 삭제했다 — `Cancelled`는 `TaskStatus`에 이미 별도 top-level `Cancelled { reason }` variant가 있어 애초에 `TaskStatus::Failed(TaskFailure{kind: Cancelled})` 경로가 도달 불가능한 설계 중복이었고, `Timeout`은 `WorkerEvent::Failed`가 여전히 구조화되지 않은 `error: String` 하나뿐이라(로드맵 #67의 fencing/lease 작업과 함께 다룰 문제) 지금 만들어도 실제로 채워질 방법이 없었고, `AuthFailed`(OIDC 토큰 검증)는 task 실행 경로에 OIDC 검증 자체가 아직 없다. `FailureKind::ALL`(4-variant 고정 배열)과 `as_str()`을 추가해 metric label이 직렬화 표현과 갈라지지 않게 컴파일러가 강제하도록 했다. 신규 테스트: `fleet-core::task::failure_kind_as_str_matches_serde_snake_case_for_every_variant`(`ALL` 순회로 신규 variant 추가 시 label 누락을 컴파일 타임에 방지하지는 못하지만 값 불일치는 테스트로 잡는다), `fleet-api::metrics::failed_tasks_are_broken_down_by_failure_kind`. 검증: `cargo test --workspace`, `DATABASE_URL` 재실행 불필요(스키마 변경 없음 — `FailureKind`는 JSONB로 저장되는 `TaskStatus`의 내부 필드라 마이그레이션 대상이 아님), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0 |
| #71 | Task dispatch credential precondition | P1 · 완료 | 이 행 (구현 요약: `crates/fleet-scheduler/src/selector.rs`의 worker 후보 선택 단계에 credential 매칭 필터를 추가했다. 로드맵 설계 초안은 `Task.resolved_model` 기준을 제안했으나 구현 중 코드 대조 결과 두 가지 이유로 `Task.model` 기준으로 조정했다: (1) `dispatcher.rs`의 `DispatchRequest.model`(워커에 실제로 전달되는 필드)이 `task.model`이지 `resolved_model`이 아니고, (2) `HeuristicTaskRouter::resolve_routing`이 `model` 미지정 task에도 항상 `resolved_model`을 채우므로 이를 기준으로 삼으면 일반 task까지 credential 보유 워커로 강제 제한해 기존 동작과 테스트 스위트 대부분을 깨뜨린다. `task.model`이 `Some`이면 `Store::get_worker_credential(worker.name, model)`이 `Some`을 반환하는 worker만 후보로 남기고(label 필터와 동일 자리), 후보가 전부 제외되면 `SelectionError::NoWorkerForCredential`을 반환한다. `Dispatcher::dispatch_existing`은 재시도 비활성(즉시 실패) 경로에서 이 에러를 `FailureKind::CredentialMissing`으로 매핑하고, `Reconciler`는 재시도 소진 dead-letter 직전 selector를 한 번 더(부작용 없이) 호출해 같은 방식으로 분류한다. `model`이 없는 task는 이 검사를 건너뛴다.) | `crates/fleet-scheduler/src/selector.rs`의 `select_credential_required_and_present_routes_normally`/`select_credential_missing_on_all_candidates_errors`/`select_credential_partial_provisioning_routes_to_credentialed_worker`/`select_no_model_skips_credential_check`, `crates/fleet-scheduler/src/dispatcher.rs`의 `submit_marks_credential_missing_when_worker_lacks_credential`/`submit_selects_worker_when_credential_present`, `crates/fleet-scheduler/src/reconcile.rs`의 `stale_pending_task_dead_letters_as_credential_missing_when_no_worker_has_credential`, `crates/fleet-core/src/task.rs`의 `failure_kind_credential_missing_snake_case` |
| #72 | Admin API bearer token DB 기반 rotate/revoke | P1 · 완료 | [보안 모델](../security/control-plane-security-model.md) | `#60`과 동일 패턴(digest 저장, 1회 원문 응답, rotate 즉시 이전 값 무효화)을 admin bearer 토큰에 적용했다. `admin_api_tokens` 테이블(`principal_id` PK, `token_digest` UNIQUE, `capabilities`, `rotation_generation`), `POST /v1/admin/tokens`(생성)·`POST /v1/admin/tokens/:principal_id/rotate`·`DELETE /v1/admin/tokens/:principal_id`·`GET /v1/admin/tokens`(메타데이터만) API, 신규 `admin_token:manage`/`admin_token:list` capability(bootstrap token 전용 `token:*`과 분리)로 fail-closed 매핑했다. `sync_env_admin_tokens_to_store`가 서버 기동 시 `FLEET_API_TOKENS`의 각 항목을 멱등하게 DB로 1회 upsert해, `auth_middleware`가 env 목록과 DB 활성 digest를 모두 검사하는 무중단 전환을 구현했다. `fleet admin-tokens create/rotate/revoke/list` CLI 추가. `FLEET_GMAIL_APP_PASS`류 제3자 발급 시크릿은 범위 밖(문서에 명시) | `crates/fleet-api/tests/admin_token_rotation.rs`(생성→인증 성공, rotate 후 이전 토큰 401·새 토큰 200, revoke 후 401, capability 분리로 403, list에 원문·digest 미노출, env→DB 자동 전환 후에도 env 토큰 인증 유지), `crates/fleet-store/tests/admin_token_rotation.rs`(PgStore 대상 create/rotate/revoke/list, 중복 principal_id Conflict, 존재하지 않는 principal NotFound) |
| #73 | HTTP capability 행렬 기본 deny 전환 | **완료** | [Authorization](../security/authorization-and-audit.md) | `crates/fleet-api/src/app.rs`의 `authorize_http_endpoint`가 `required_capability`의 `None`을 `403`으로 처리하도록 전환했다. `/health`와 `POST /workers/join`(body의 bootstrap token이 자체 인증 수단)만 명시적으로 허용한다. 과거 누락이었던 `GET /v1/workers/{id}`(신설 `worker:list` 매핑)와 `POST /hosts/register`(신설 `host:provision` 매핑)를 행렬에 등록했다 — 전자는 워커의 ACP `server-key`를 담은 `endpoint` 필드를 무권한 노출했고, 후자는 기존 Host의 `ssh_host`/`ssh_user`/`status`/`worker_id`를 무권한으로 덮어썼다. **범위(Dashboard `/api`) 미착수**: `crates/fleet-dashboard`에는 중앙 capability 행렬이 아예 없고 핸들러에 `PermissionKind` 검사가 29곳 산재한다 — `#86`~`#93` 관리 화면이 이 표면에 놓이므로 `#92`로 넘긴다. 검증: `crates/fleet-api/src/app.rs`의 `capability_matrix_covers_router_routes`/`authorize_http_endpoint_denies_by_default_for_any_unmatched_route`/`get_worker_by_id_and_host_register_now_require_capability`/`is_worker_by_id_route_matches_single_segment_only`, `crates/fleet-api/tests/capability_matrix_default_deny.rs`(8건, 실제 HTTP 스택 — 이전에 뚫려 있던 두 route가 이제 403/200을 정확히 내는지, 워커 operational credential로 다른 워커·Host를 건드릴 수 없는지, `allow_no_auth` 모드에서도 join이 여전히 동작하는지) |
| #74 | Cloudflare principal capability 매핑 fail-closed | **완료** | [Authorization](../security/authorization-and-audit.md), [보안 모델](../security/control-plane-security-model.md) | `crates/fleet-api/src/app.rs::cf_access_capabilities`가 매핑 부재(`cf_principal_capabilities == None`) 시 `PermissionKind::all()`을 반환하던 것을 빈 `Vec`으로 고쳤다 — CF Access application 정책만 통과하면(principal이 누구든) 모든 워커의 LLM 프로바이더 API 키 원문 export와 admin token 발급까지 가능했던 실제 결함이었다. `fleet-cli`에 매핑 설정 경로를 신설했다 — `Command::Serve`의 `--cf-principal-capabilities`/`FLEET_CF_PRINCIPAL_CAPABILITIES`(JSON 배열 `[{"email":...,"capabilities":[...]}]`, `ApiTokenCredential`/`FLEET_API_TOKENS`와 같은 파싱·검증 스타일이나 `token` 필드는 없음 — CF JWT가 이미 신원을 검증하므로 별도 bearer secret이 불필요)가 `AppState::with_cf_principal_capabilities`를 호출한다(이전에는 이 빌더의 유일한 호출부가 테스트뿐이었다 — 조사로 재확인). `run_serve`가 `FLEET_CF_AUDIENCE`는 설정됐는데 매핑이 비어 있으면 non-loopback 무인증 bind 거부와 같은 원칙으로 기동 자체를 거부한다 — fail-closed로 조용히 기동해 모든 요청이 이유 없이 403을 받는 배포를 방치하지 않는다. 기존에 fail-open을 직접 assert하던 테스트 3건(`cf_capabilities_default_to_all_when_unmapped_deployment`, `cf_access_without_capability_mapping_keeps_full_access`, 그리고 우연히 여기 편승했던 `cf_access_accepts_valid_jwt`/`cf_access_case_insensitive_header`)을 fail-closed 기준으로 뒤집거나 명시적 매핑을 쓰도록 고쳤다. 게이트 검증: 실제 빌드한 바이너리로 (1) `FLEET_CF_AUDIENCE`만 설정하고 매핑 없이 기동 시 SSH/HTTP 어떤 처리도 시작하기 전에 즉시 거부됨을, (2) 매핑을 함께 주면 "fail-closed capability mapping" 로그와 함께 정상 기동함을 실측했다. 신규/변경 테스트: `crates/fleet-api/src/app.rs`(단위 1건 반전), `crates/fleet-api/tests/cloudflare_access.rs`(1건 반전+2건 명시 매핑으로 전환), `crates/fleet-cli/src/runtime.rs::cf_principal_capabilities_tests`(8건 — 유효 매니페스트, 다중 principal, 빈 배열·빈 email·빈 capabilities 거부, 잘못된 JSON, capabilities 필드 누락, email 정규화는 이 함수 책임이 아님을 고정) |
| #75 | Worker endpoint secret 마스킹(1단계) | **완료** | [Enrollment](../contracts/worker-enrollment.md) | 이 항목이 원래 요구한 두 단계 중 **1단계(응답·이벤트 마스킹)만** 완료로 좁혔다 — 2단계(ACP 인증을 URL query 밖으로 이전)는 `grok agent serve`(이 저장소 밖 외부 프로세스)가 실제로 어떤 인증을 강제하는지 확인해야 안전하게 설계할 수 있어 `#94`로 분리했다(아래). `agent_endpoint`의 `?server-key=` 값이 `workers.endpoint`를 거쳐 `GET /v1/workers/{id}`·Dashboard `/api/workers`·`/api/events`·MCP `fleet_list_workers`로 전파되던 네 경로를 전부 닫았다 — 신설 `fleet_core::mask_server_key`(원래 `fleet-transport::acp_transport::sanitize_endpoint`가 로깅용으로만 갖고 있던 것과 동일한 경계 탐지 로직을 `fleet-core`로 승격해 중복을 없앴다)로 `?server-key=<값>`을 `<redacted>`로 치환한다. 이벤트 로그(`FleetEvent::WorkerJoined`)는 append-only라 **쓰기 시점**(`register_worker`/`join_worker` 핸들러가 이벤트를 구성할 때)에 마스킹한다 — 읽기 시점 필터링만 하면 DB에는 원문이 영구히 남는다. transport 등록 실패 시의 `tracing::warn!` 로그 2곳도 함께 마스킹했다(로드맵 원문엔 없었지만 조사 중 발견한 같은 성격의 노출). `fleet-transport::acp_transport::register`/`build_ws_client`(실제로 워커에 다이얼하는 유일한 정당한 소비자)는 그대로 원문을 쓴다 — 확인 후 변경하지 않았다. 검증: `fleet-core::worker::tests`의 `mask_server_key_*` 6건(쿼리 값 치환·스킴/호스트/경로 보존·뒤따르는 다른 파라미터 보존·fragment 보존·secret 없는 값은 그대로·빈 문자열), `fleet-api/tests/capability_matrix_default_deny.rs`의 `get_worker_by_id_never_leaks_raw_server_key_even_with_capability`(인가된 호출자에게도 원문 미노출 — `#73`의 인가 테스트와는 다른 계층)·`register_worker_writes_masked_endpoint_into_the_event_log`(저장소를 직접 읽어 이벤트 컬럼 자체가 마스킹돼 있음을 확인), `fleet-api/tests/transport_integration.rs`의 `registered_worker_endpoint_is_masked_on_read_even_though_transport_got_the_raw_value`(같은 요청 안에서 transport는 원문을, HTTP 응답은 마스킹된 값을 받음을 동시에 확인), `fleet-dashboard/tests/dashboard_api.rs`의 `workers_list_never_leaks_raw_server_key`, `fleet-mcp/src/handlers.rs`의 `worker_summary_never_leaks_raw_server_key` |
| #94 | ACP 인증을 URL query 밖으로 이전 | **완료 (2026-08-26)** | [Enrollment](../contracts/worker-enrollment.md) | `#75`에서 분리된 2단계. **착수 전 확인 사항의 답이 로드맵 원문과 달랐다**: 이 항목은 원래 "mTLS client cert만으로 충분한지"를 게이트로 걸었는데, 그건 **구조적으로 불가능**하다 — `fleet-transport`의 `MtlsProxy`가 TLS를 종단하고 grok에는 평문 TCP를 넘기므로(`copy_bidirectional`) grok은 client certificate를 애초에 볼 수 없다. 실측으로도 로컬의 grok 세 버전(0.2.102 / 0.2.112 / 1.0.5)이 전부 인증 없는 `/ws`를 거절했다. 그래서 이 항목이 할 수 있는 것은 secret의 **이전**이지 **제거**가 아니며, 제거는 grok 자체의 변경(범위 밖)을 요구한다 — 게이트 문구를 그에 맞게 고쳤다. **저장소는 한 줄도 바꾸지 않았다**: secret은 이미 `Worker.endpoint` 문자열 안에 있고 `#75`의 `mask_server_key`가 네 개 읽기 경로를 지키고 있으므로, 새 컬럼을 만들면 그 네 경로를 전부 다시 열고 새 마스킹 의무를 만드는 것 — 유출면을 줄이는 게 아니라 **옮기는** 것이다. 대신 신설 `fleet_core::split_server_key`가 **다이얼 직전 단 한 곳**(`acp_transport.rs::build_ws_client`, `HttpClient::with_endpoint`의 나머지 두 호출부는 OTLP span exporter라 무관함을 확인)에서 URL을 쪼개 `Authorization: Bearer <secret>` 헤더로 옮긴다. 생산자(`WorkerConfig::agent_endpoint`, `join_worker.rs::mtls_agent_endpoint`)를 건드리지 않았으므로 이 변경 전후에 등록된 워커가 동일하게 동작하고 혼재 상태 마이그레이션이 없다. `split_server_key`는 `mask_server_key`와 달리 **보수적**이다 — 마커 앞이 `?`/`&`가 아니면 손대지 않는다(마스킹은 과하게 가려도 안전하지만 잘못 쪼개면 다이얼 자체가 깨진다). 벤더 크레이트(`agent-client-protocol-http`)에는 `HttpClient::with_ws_auth_header`와 `run_ws`의 다이얼 인자를 `&str` → `IntoClientRequest`로 바꾼 패치를 넣고 `FLEET_PATCHES.md`에 기록했다(`Debug`는 값이 아니라 **존재 여부만** 찍는다). `FLEET_ACP_AUTH=query`로 예전 동작으로 되돌릴 수 있다 — 세 버전이 major 경계를 가로질러 전부 헤더를 받아들였으므로 필요할 가능성은 낮지만, 관측되지 않은 배포를 위해 프로세스 단위 스위치 하나만 남기고 **워커별 자동 협상은 만들지 않았다**(헤더를 거절하는 grok이 하나도 관측되지 않았고, 가정 위에 협상 기계를 지으면 트리거되지 않는 분기가 영구히 남는다). `ws_auth_parts(endpoint, mode)`를 순수 함수로 분리한 이유는 `FLEET_ACP_AUTH`가 프로세스 전역 상태라 병렬 테스트를 오염시키기 때문이다 — env 읽기는 `AcpAuthMode::from_env()` 한 곳에 가둔다. **이 항목이 실제로 닫는 구멍**: 역프록시(nginx 등)가 요청 라인을, 따라서 쿼리 문자열을 기본 로그 포맷에 평문으로 남기지만 헤더는 남기지 않는다 — 우리가 소유하지 않는 로그에 secret이 쌓이는 것을 막는다. 신규 테스트: `fleet-core::worker::tests`의 `split_server_key_*` 8건(단독 쿼리 제거·앞/뒤 다른 파라미터 보존·fragment 보존·secret 없는 값·쿼리 파라미터가 아닌 마커 무시·빈 값·결과가 `url::Url`로 파싱되고 secret 문자열이 남지 않음), `fleet-transport::acp_transport`의 5건(헤더 모드 이전, 헤더 값 형식 고정, query 모드는 저장값 그대로, secret 없는 endpoint는 양쪽 모드에서 불변, 비mTLS 명명 경로가 path를 잃지 않음), `fleet-transport/tests/mtls_proxy.rs::mtls_proxy_forwards_authorization_header_verbatim`(mTLS 프록시 홉이 헤더를 그대로 넘기는지를 상류에서 **캡처해** 확인 — "당연해 보인다"와 "측정했다"는 다르다), `fleet-transport/tests/e2e_with_real_grok.rs::e2e_94_secret_travels_in_header_not_url`(실제 `grok agent serve`에 다이얼하는 이 항목의 완료 게이트. **음성 대조를 같은 프로세스에서 함께 돌린다** — 인증 없는 `/ws`가 거절되는 것을 먼저 확인하지 않으면 양성 통과가 "헤더가 인증됐다"인지 "grok이 인증을 강제하지 않았다"인지 구분되지 않는다). **미검증**: 비mTLS 워커의 `/ws/{name}` nginx 홉이 `Authorization`을 상류로 전달하는지는 그 설정이 운영자 소유이고 이 저장소 밖이라 확인하지 못했다(`proxy_set_header`로 지운 배포는 조용히 401을 받는다). grok 0.2.102 미만은 로컬에 바이너리가 없어 미테스트. 실제 프로덕션 워커에 디스패치를 돌려 본 것이 아니라 로컬 grok 프로세스가 대상이다. 미룬 것 전체는 Enrollment 정본의 표에 있다. **검증**: 위 신규 테스트 15건 + 기존 `mask_server_key_*` 6건 회귀. 게이트: `cargo fmt --all -- --check`, `cargo clippy --workspace --features "acp mtls" --all-targets -- -D warnings`, `cargo clippy --workspace --no-default-features --all-targets -- -D warnings` 각각 종료 코드 0을 단계별로 확인(체인의 마지막 코드만 보면 중간 실패를 놓친다 — 오늘 실제로 그렇게 한 번 속았다), `cargo test --workspace`, `DATABASE_URL` 주입 통합 테스트 직렬 재실행 |
| #76 | 감사 범위 확장 (mutation·capability 거절) | **완료 (2026-08-23, 1단계)** | [Authorization](../security/authorization-and-audit.md) | 이번에 닫힌 범위: bootstrap token 발급·회수(`token.bootstrap.issue`/`.revoke`), admin API token 발급·회전·회수(`admin_token.create`/`.rotate`/`.revoke`), Worker 등록·등록해제(`worker.register`/`.deregister`, heartbeat는 고빈도라 제외), Host 등록(`host.register`), HTTP capability 거절(`http.capability_denied`) 전부가 이제 `AuditEvent`를 남긴다 — 과거엔 이 7개 mutation과 모든 capability 거절이 `tracing::warn!`으로만 남아 사후 조회가 불가능했다. `worker_llm_credential_export`(`#66`)의 "감사 실패 시 거절" 원칙을 발급(mint) 쪽에도 적용했다: bootstrap/admin token 발급·회전은 감사 기록이 실패하면 방금 만든 자격을 즉시 회수하고 500을 반환한다(rotate는 이전 토큰을 복원할 수 없으므로 새 토큰마저 회수해 principal을 무자격 상태로 안전하게 실패시킨다 — 권한이 남는 쪽보다 없는 쪽으로 실패). 반대로 이미 반영된 mutation(회수·등록·등록해제·capability 거절)은 감사 실패로 응답을 뒤집지 않는다(log-only) — `delete_worker_credential`과 같은 원칙. capability 거절 감사는 `auth_middleware`의 5개 인증 분기 전부(개발 무인증·worker operational·admin DB 토큰·CF Access·env bearer)에서 `authorize_http_endpoint`가 `Err`를 반환할 때 기록되며, `axum::http::Request`가 `Sync`가 아니라 `&Request`를 async fn 시그니처에 남기면 미들웨어 future가 `Send`를 잃는다는 걸 컴파일 에러로 발견해 필요한 값(method/path/`AuthorizationContext`)만 owned로 추출해 넘기도록 고쳤다. `MemStore::with_failing`(기존 회복성 테스트 인프라)에 `"record_audit_event"`를 추가해 감사 실패를 실제로 주입하는 테스트를 가능하게 했고, `bootstrap_tokens.rs`의 기존 minimal `BsStore`(`record_audit_event` 미구현 → `Store` 트레이트 기본값인 `Unsupported`)가 이번 fail-closed 변경으로 깨지는 걸 발견해 실제 audit override를 추가했다 — 감사 미구현 Store로 프로덕션에 배포하면 이제 발급류 mutation이 전부 500이 된다는 뜻이므로 실배포 전 반드시 확인해야 한다. `request_id`·`project_id`·`attempt_id`·`policy_revision` 상관관계 필드와 Dashboard/MCP 표면의 동일 감사는 `#95`로 분리했다 — `project_id`/`attempt_id`는 대응하는 Project/Attempt 엔티티(`#48` 계열)가 아직 없어 상관시킬 대상이 없다. 신규 테스트: `crates/fleet-api/tests/audit_coverage.rs` 9건(발급/회전/회수 감사, 발급·회전 fail-closed 2건, 등록·등록해제·host 등록 log-only, capability 거절 감사, 거절 감사 실패가 403 응답을 바꾸지 않음을 대칭 확인). 검증: `cargo test --workspace`(96 스위트 전체 통과), `DATABASE_URL` 주입 `fleet-store`/`fleet-api` 재실행(26 스위트 전체 통과), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0 |
| #95 | 감사 상관관계 필드와 Dashboard/MCP 감사 | P2 · 설계 필요 | [Authorization](../security/authorization-and-audit.md) | `#76`에서 분리된 2단계. `AuditEvent`에 `request_id`(HTTP 요청 단위로 여러 감사 이벤트를 묶는 상관관계 키)·`project_id`·`policy_revision`을 추가한다 — `attempt_id`는 아래 이유로 **이 목록에서 제외했다**. `project_id`는 착수 전 반드시 확인할 것: 이 저장소에 아직 Project 엔티티가 없다(`#48` 계열 선행) — 상관시킬 대상이 생기기 전에 필드만 먼저 추가하면 항상 `NULL`인 죽은 컬럼이 된다. **`attempt_id`를 뺀 이유**: `#62` 4단계(2026-08-26)가 무재시도 정책 아래에서 Task와 시도가 1:0..1임을 확인하고 `TaskAttempt`를 만들지 않기로 결정했으므로, "Attempt 엔티티가 생기면 상관시킨다"는 대기 사유는 **`#62`로는 영원히 충족되지 않는다**. 상관 대상은 `task_id`이며 그것은 이미 있다. [`authorization-and-audit.md`](../security/authorization-and-audit.md) 202-204의 같은 서술은 `#97`(2026-08-26)이 이미 고쳤다 — 그 문장은 이 항목의 남은 범위가 아니다. `policy_revision`도 마찬가지로 policy revision 개념 자체가 아직 없다. `request_id`는 독립적으로 착수 가능하다(HTTP 요청 진입 시 생성해 미들웨어 확장에 심고, 핸들러의 모든 `AuditEvent`에 전파). `#76`이 남긴 gap: `crates/fleet-dashboard`에는 중앙 capability 행렬이 없어(`#73`의 fail-closed 불변식도 아직 없음) capability 거절 감사를 추가할 지점이 불명확하고, MCP 표면의 tool별 감사도 착수 전이다 — `#92`(AgentTemplate·Issue 관리 표면)가 Dashboard 표면을 다루므로 그 설계와 함께 결정한다 |
| #96 | Task 삭제와 스레드 그룹 목록 | **완료 (2026-08-26)** | [Task Management](../architecture/tasks/management.md), [UI 설계](../ui-dashboard/ui-design.md), [Dashboard API](../contracts/dashboard-api.md) | terminal Task 삭제와, 태스크 목록을 최초 태스크 아래 하위 목록으로 묶는 그룹 UI. **설계 확정 사항**: ① 캐스케이드는 **새로 정할 것이 없다** — 스키마가 이미 다섯 개 FK 절로 결정해 뒀다(`task_outputs`·`task_telemetry` CASCADE, `events`·`tasks.parent_task_id`·`issue_task_links` SET NULL). 그래서 soft delete를 쓰지 않는다 — 쓰면 그 설계가 전부 죽은 코드가 된다. ② terminal만 삭제 가능하고, 판정은 선검사가 아니라 `DELETE ... AND status_phase = ANY($2)`의 0행으로 한다 — 읽기와 삭제 사이에 `Dispatched → Completed`가 끼어들 수 있어(두 writer, 공유 트랜잭션 없음) 선검사는 `#62` 1단계가 없앤 것과 같은 TOCTOU를 되살린다. ③ `Dispatched` 삭제를 막는 이유는 정합성이 아니라 **침묵**이다 — CAS 덕에 저장소는 멀쩡하지만(`UPDATE` 0행 → 재조회 `None` → `Err(NotFound)`) 디스패처의 `Err` 갈래는 `warn!` 한 줄뿐이라 워커가 머신을 끝까지 태운 결과가 아무 기록 없이 사라진다. ④ `Pending` 의존자가 있으면 삭제를 거부한다 — `dependency_ids`는 `UUID[]`이고 **FK가 없어** DB가 막아 주지 않으며, dispatch 준비 판정은 선행 조회가 `Ok(None)`이면 `ready = false`로 끝내는데 없는 행은 영영 생기지 않아 의존자가 dead-letter도 timeout도 없이 `Pending`에 **영구히 갇힌다**. terminal 의존자는 검사하지 않는다(이미 실행이 끝나 ready 판정을 다시 지나지 않으며, 전부 검사하면 DAG가 조금만 깊어도 삭제가 불가능해진다). ⑤ 멱등성 키는 삭제 시 **해제된다** — 게이트 3의 보장은 "중복 제출은 *기존 Task를 반환한다*"이므로 반환할 Task가 없어진 뒤의 tombstone은 조회하면 404가 되는 id를 건네는 것이라 보장을 지키는 게 아니라 더 나쁘게 깨뜨린다. ⑥ 그룹은 "루트 Task의 행"이 아니라 **`thread_id` 값 자체**다 — `parent_task_id`가 `ON DELETE SET NULL`이라 루트가 삭제된 스레드가 도달 가능한 정상 상태이고, 루트 기준으로 잡으면 그 자식들이 `id != thread_id`라 어느 그룹에도 못 붙어 목록에서 통째로 사라진다. 값 기준으로 잡으면 그저 "헤더 없는 그룹"이라 특수 경로가 없다. ⑦ 페이지 단위도 스레드다(Task 단위면 그룹이 경계에서 잘린다). **필요한 마이그레이션은 하나**: `dependency_ids`의 부분 GIN 인덱스(`WHERE dependency_ids <> '{}'`) — 의존자 검사가 `@>` 포함 질의를 요구하는데 이 컬럼엔 인덱스가 없다(저장소 전체 GIN은 `workers.labels` 하나뿐). 권한 `TaskDelete`(Admin 전용, `WorkerDelete` 관례)는 permission이 코드 정의이고 매 기동 재시드되므로 마이그레이션이 **필요 없다**. 그룹 질의도 `idx_tasks_thread_id (thread_id, created_at)`가 이미 있어 필요 없다. `task.*` 감사 액션이 **하나도 없어** `task.delete`를 새로 만든다 — hard delete는 행을 없애지만, 남는 `events` 행은 `task_id` 컬럼만 NULL이 될 뿐 `payload`(FleetEvent 전체를 담은 JSONB)에 원본 `task_id`가 그대로 남아 신원을 잃지 않는다(단, `task_id` 컬럼을 읽는 조회 경로가 코드에 없어 인덱스 없는 스캔이 아니면 조회할 수 없다). 그래서 "그 Task가 존재했고 누가 지웠다"를 **조회 가능한 형태로** 증언하는 것은 감사 로그뿐이다 — `events`가 정보를 안 담아서가 아니라, 감사 로그만 인덱스가 있는 자리에 남기 때문이다. 의존자 조회는 `dependency_ids <> '{}' AND dependency_ids @> ARRAY[$1]`을 함께 써야 부분 GIN 인덱스를 planner가 실제로 탄다(`@>` 단독으로는 인덱스 조건을 증명하지 못한다). 게이트는 management.md의 삭제 계약 절 하단 6개(전부 테스트로 닫음). **구현(이번)**: `Store::delete_task`(PgStore/MemStore, `TaskDeleteOutcome` 3분기)와 `Store::list_task_threads`(기본 구현은 `list_tasks` 위에서 유도, PgStore는 `GROUP BY thread_id ORDER BY MAX(created_at) DESC` 집계 질의로 재정의), `PermissionKind::TaskDelete`(admin 전용, Operator/Viewer 목록에 넣지 않는 것이 유일한 차단 지점), `audit::action::TASK_DELETE`, `DELETE /api/tasks/:id`·`GET /api/task-threads` 핸들러(성공·거부 모두 감사 기록), migration `025_task_dependency_gin.sql`(부분 GIN, `dependency_ids <> '{}' AND dependency_ids @> ARRAY[$1]`을 함께 써야 플래너가 predicate를 증명), 대시보드 Task 목록을 스레드 헤더 + 들여쓴 구성원으로 그룹화하고 collapse/expand·삭제 버튼(terminal + 권한 게이트)·삭제 확인 다이얼로그·`(최초 태스크 삭제됨)` placeholder·`n / m 표시 중` 부분 필터 배지를 추가한 프런트엔드(`tasks.js`/`tasks.html`/`styles.css`). 신규 테스트: `crates/fleet-store/tests/task_delete.rs` 8건(게이트 1~4, MemStore/PgStore 양쪽 — `delete_task_cascades_outputs_and_telemetry_and_nulls_event_task_id_column`이 실제로 Task를 지운 뒤 `events.task_id` 컬럼은 NULL, `payload.task_id`는 원본 보존임을 실행으로 확인), `crates/fleet-dashboard/tests/dashboard_api.rs`에 9건 추가(terminal 삭제·비terminal 409·404·의존자 차단 409·권한 403·CSRF 거부·스레드 그룹핑·삭제된 루트 생존·감사 기록 성공/거부 양쪽). 검증: `cargo fmt --check`, `cargo clippy`(acp+mtls, no-default-features 둘 다 `-D warnings`) 경고 0, `cargo test --workspace`(68 스위트 전체 `ok`), `DATABASE_URL` 주입 `fleet-store`(14 바이너리)·`fleet-api`(14)·`fleet-scheduler`(2)·`fleet-mcp`(1, `fleet-cli --all-features` 재빌드 후)·`fleet-dashboard`(2) 전부 `--test-threads=1`로 바이너리별 격리 실행해 전체 통과(이 크레이트들의 DB 테스트는 같은 Postgres를 `TRUNCATE ... CASCADE`로 공유해 병렬 실행 시 서로 경합한다 — `docs/log.md` 2026-08-25 항목이 남긴 하네스 제약). 실제 브라우저로도 로그인 후 그룹핑·들여쓰기·collapse/expand·삭제·삭제된 루트 placeholder·부분 필터 배지를 눈으로 확인했다 — 단 스레드/부모 행은 실제 제출 경로가 아니라 raw SQL로 시딩했고, 헤드리스 브라우저가 `window.confirm`을 자동 거부해 `window.confirm = () => true`로 오버라이드한 뒤 재클릭해 확인 후 경로를 exercised했다(다이얼로그 문구 자체는 화면에서 읽지 못함) |
| #97 | `TaskAttempt` 목표 엔티티 존치 여부 재판정 | P2 · 완료 (2026-08-26) | [흡수 판정](../architecture/project-task-agent-lifecycle.md#attempt-흡수-판정) | **판정: 흡수.** 근거·용어 대응표·교차 도메인 결과는 정본이 소유하므로 여기 옮기지 않는다. 게이트: (a) 판정을 Lifecycle 계약에 정본 기록 — 닫힘. (b) 실측 30개 중 reviews 5개를 뺀 25개가 판정과 일치 — 닫힘(집계표 소유 파일까지 26개 수정). (c) `#95`의 `attempt_id` 제외와 무모순 — 닫힘. 무모순을 넘어 [`authorization-and-audit.md`](../security/authorization-and-audit.md) 202-204가 "상관 필드는 `task_id`이며 이미 있다"를 적극 단언하도록 고쳤으므로 **`#95`에서 그 문장을 고칠 몫은 사라졌다**. (d) [재작성 가이드](../governance/documentation-rewrite-guide.md) 게이트 — 닫힘: 링크·앵커 파손 0, Mermaid 27블록에 `Attempt` 노드 잔존 0, frontmatter 26개 갱신. **계약이 실제로 바뀐 곳은 한 군데다** — credential grant 수명을 "Task 단위"로 평평하게 옮기면 유효 구간이 넓어지므로 [보안 모델](../security/control-plane-security-model.md)과 [credentials 진입점](../credentials/README.md)에 "dispatch~terminal 실행 구간"으로 명시했다. 나머지는 개명이다. **선행을 기다리지 않았다**: 이 행은 원래 `#48` 계열과 함께 판정하라고 적었으나 후보 3경로가 현재 정본 문언만으로 판별돼 먼저 판정했다 — `#48`이 Project를 만들 때 어긋나는 사실이 나오면 재개정한다. reviews 5개 파일은 결정 당시 근거이므로 소급 수정하지 않았다. **검증 한계**: 문서 전용이라 코드로 증명할 대상이 없다. sweep·링크·Mermaid 노드 검사는 스크립트로 돌렸으나 의미 정합성은 사람 판독뿐이고, Mermaid는 렌더러로 파싱시키지 않았다 |

### 무인 부트스트랩과 배포 자동화 (#77-#85, 전체 완료)

2026-08-22 [무인 부트스트랩 검토](../reviews/bootstrap-automation-review-2026-08-22.md)에서 도출한
항목이다. 순서는 근거를 가졌다 — `#77`은 이후 모든 검증의 전제, `#78`은 이미 운영 중인 fleet을
망가뜨리는 유일한 항목, `#79`는 없으면 이후 스텝의 성공 여부를 관측할 수 없다. `#82`는 `#79`와
`#80`에, `#85`는 `#82`(join 배선)와 `#84`(안전한 원격 파일 전송)에 의존했다. 2026-08-23에 `#85`
완료로 9개 항목이 모두 닫혔다 — `fleet provision`이 seed worker부터 임의 아키텍처·mTLS 활성화
워커까지 사용자 개입 없이 등록·기동시킬 수 있다.

| ID | 항목 | 우선순위·상태 | 정본 | 완료 게이트 |
|---|---|---|---|---|
| #77 | 배포 예시를 현재 파서에 정합 | 완료 | 이 행 | `examples/fleet.env`의 `FLEET_API_TOKENS`를 `{principal_id, token, capabilities}` JSON 배열로 교체하고 capability 최소권한 안내를 붙였다(`worker:llm_credential:export`는 기본 예시에서 제외). `examples/fleet.service` 주석의 `token1,token2`도 같은 형식으로 교체. `examples/fleet-worker.service` 헤더의 "`fleet provision`이 자동 배포한다" 서술을 정정했다 — 프로비저너가 실제로 쓰는 것은 `templates.rs`의 `FLEET_WORKER_UNIT`(User=root, 하드닝 없음)이며 두 형상 일치는 별도 항목이다. 검증: `crates/fleet-api/tests/verify_env_example.rs`의 `examples_fleet_env_api_tokens_parse_as_manifest`(파서와 동일 조건으로 manifest 파싱·필드 비어있음 거부), `examples_fleet_env_does_not_grant_credential_export_by_default` |
| #78 | Graceful shutdown hard-delete 중단 | 완료 | 이 행 | `crates/fleet-worker/src/runner.rs`의 shutdown 경로에서 `client.deregister()` 호출을 제거했다. 그 요청은 `DELETE /v1/workers/{id}` → `DELETE FROM workers`로 이어져 `worker_operational_credentials`(018 CASCADE)와 `worker_credentials`(005 CASCADE, 암호화된 LLM 키)를 함께 파괴했고, 인증이 구성된 모든 배포에서 `systemctl restart` 한 번이 워커를 영구 401로 만들었다. "이 워커는 이제 없다"는 신호는 `fleet-scheduler`의 HealthChecker가 heartbeat timeout으로 Offline 전이와 `WorkerLeft` 이벤트를 내며 비파괴적으로 담당한다. 영구 제거는 관리자의 `DELETE /v1/workers/{id}`만 수행하고, `WorkerClient::deregister` 메서드는 그 경로용으로 유지했다. `MemStore::delete_worker`를 PgStore와 동작 일치시켰다 — 두 credential 테이블을 함께 제거하고 존재하지 않는 id에 `NotFound`를 반환한다(이 divergence 때문에 결함이 모든 인메모리 테스트를 통과했다). `config.rs`의 legacy 거부 에러가 안내하던 `credential rotate` 복구 경로가 404임을 명시하도록 정정. 검증: `crates/fleet-store/src/mem.rs`의 `delete_worker_cascade_tests`(2종), `crates/fleet-store/tests/worker_delete_cascade.rs`(2종, 실제 PostgreSQL에서 두 CASCADE와 무관 워커 자산 보존 확인) | **후속**: `WorkerConfig`의 `rotation_generation` 제약(PG는 `>= 1` CHECK, MemStore는 미강제)이라는 별도 divergence를 발견했다 — 추적 필요 |
| #79 | 원격 실행 실패 관측 가능화 | **완료** | 이 행 | `crates/fleet-provisioner/src/ssh.rs`의 `RemoteExecutor`에 `exec_checked` 기본 메서드를 추가했다 — `exec_streaming`을 위임해 exit code를 관측하고 비0이면 `StepError::RemoteExit`로 승격한다(`exec()` 자체는 `test -f ... && echo yes` 같은 조회 명령을 위해 exit code 무시를 유지). `install_fleet_worker.rs`·`push_credentials.rs`·`install_cloudflared.rs`의 `let _ =`/`|| true`로 버려지던 mutation(`sudo mkdir`, `sudo mv`, `daemon-reload` 등)을 `exec_checked`로 교체했다. 구현 중 `install_cloudflared.rs`에서 실제 잠복 버그를 발견했다 — `/etc/cloudflared` 디렉토리를 만드는 스텝이 없어 새 호스트에서 `config.yml` `mv`가 조용히 실패하고 있었다(스텝은 "Applied"로 보고됨); `sudo mkdir -p /etc/cloudflared`를 추가했다. `start_services.rs`의 죽은 `wait_timeout_secs`(참조 0건)를 실제 폴링으로 구현했다: 로컬 `systemctl is-active`를 즉시 1회가 아니라 타임아웃까지 재시도하고, `orchestrator_api_token`이 있으면 `GET /v1/workers`로 워커가 `online`으로 보고될 때까지 폴링한다(토큰 없으면 로컬 확인만 했다고 경고 로그 후 진행 — 하위 호환). 401/403은 재시도해도 해소되지 않으므로 `NotFound` 타임아웃과 구분해 즉시 실패시킨다 — capability 부족을 "워커가 등록 안 됨"으로 오인시키지 않기 위해서다(이 구분 자체가 `#79`의 요지를 재확인한 발견). `PlaybookError::StepFailed`에 `completed_steps: Vec<StepReport>`를 추가하고 `fleet-cli`의 실패 처리 두 곳(`recover_completed_steps`)이 이를 써서 빈 `steps: vec![]` 대신 실제 실행 이력을 리포트에 채우게 했다. 검증: `crates/fleet-provisioner/src/playbook.rs`의 `completed_steps_includes_earlier_skipped_and_applied_steps`, `start_services.rs`의 `apply_fails_when_daemon_reload_fails`/`apply_continues_when_only_cloudflared_enable_fails`/`classify_status_*`(3종), `crates/fleet-cli/src/runtime.rs`의 `recover_completed_steps_tests`(2종) |
| #80 | 최초 admin 토큰 발급 경로 | **완료** | [보안 모델](../security/control-plane-security-model.md) | `fleet-api`에 `issue_admin_bootstrap_token_if_needed`를 추가했다 — `admin_api_tokens`가 비어 있으면(principal_id=`bootstrap`, full capability) 1개 발급하고 원문을 반환한다. 어디에도 파일을 쓰지 않는다(store 조작만, 순수 함수 성격 유지). **`sync_env_admin_tokens_to_store` 이후에 호출**해야 하는 순서 제약을 문서화했다 — env로 admin을 구성한 배포는 그 sync가 먼저 DB를 채우므로 추가 전체-권한 토큰을 몰래 심지 않는다. `fleet-cli`가 `--http-bind` 기동 시 이를 호출하고, 반환된 원문을 `write_admin_bootstrap_token_file`로 `0600` 파일(`/etc/fleet/bootstrap-admin-token`, `FLEET_ADMIN_BOOTSTRAP_TOKEN_FILE`로 오버라이드 가능)에 1회 쓴다. 저널/표준출력에는 원문을 남기지 않는다 — dashboard OTP(`tracing::info!`)와 의도적으로 다른 경로다. 파일이 이미 존재하면 덮어쓰지 않고 에러(안전장치)로, 토큰은 DB에 발급됐으나 파일화에 실패했다는 사실을 로그로 명확히 남긴다. **dashboard OTP 재사용은 채택하지 않았다** — `rbac.rs`의 `issue_admin_bootstrap_if_needed`가 `purpose`를 구분하지 않으므로(설계 검토에서 확인) 별개 경로로 구현했다. **게이트 재확인**: 로드맵 초안이 요구한 "발급 경로가 다른 토큰과 동일한 `AuditEvent`를 남김"은 구현 전 코드 대조에서 전제가 틀렸음을 확인했다 — `create_admin_token` 핸들러 자신도 `AuditEvent`를 남기지 않고 `tracing::info!`만 쓴다(감사 기록은 `#66`의 LLM credential 3개 route에만 존재, `#76` 범위). 존재하지 않는 관례에 이 항목만 맞추는 것은 과잉이므로, 같은 관측 수준(`tracing::info!`)으로 구현하고 광범위한 감사 확장은 `#76`에 남겼다. 검증: `crates/fleet-api/tests/admin_bootstrap_token.rs`(4건 — 빈 store 1회 발급, 발급된 토큰이 실제로 `/v1/workers`와 `/v1/admin/tokens` 양쪽에 인증됨, 재기동 시뮬레이션에서 재발급 없음, env sync 이후 미발급), `crates/fleet-cli/src/runtime.rs`의 `admin_bootstrap_token_file_tests`(3건 — `0600` 권한, 부모 디렉토리 생성, 기존 파일 보존) |
| #81 | 이기종 fleet 지원 (arch·OS 감지 배선) | **완료** | 이 행 | `crates/fleet-provisioner/src/steps/check_prereqs.rs`에 `detect_prereq`(원격 실제 조회)와 `assumed_prereq_from_labels`(인벤토리 라벨 기반 미리보기 가정값)를 추가했다. `fleet-cli::run_playbook`과 `fleet-dashboard::run_provisioning`이 `PrereqReport`를 하드코딩(`ubuntu`/`x86_64`)하던 자리를 실제 호출로 교체했다 — dry-run은 라벨 기반 가정값, 실 연결은 `detect_prereq`(실패를 조용히 덮지 않고 그대로 전파). `Playbook::standard(prereq)`가 이제 `InstallFleetWorker`/`InstallCloudflared`에도 `target_arch`를 주입한다(기존에는 `InstallDeps`만 받았음). **구현 중 실제 재현으로 발견한 회귀**: `CheckPrereqs::apply`는 원래 `ctx.dry_run`과 무관하게 항상 진짜 조회를 시도했다 — 인벤토리 dry-run은 아무 것도 프로그래밍되지 않은 `MockExecutor`를 쓰므로 모든 질의가 빈 문자열로 돌아와 "could not detect OS"로 **매번 실패**했다(`fleet provision --inventory ... --dry-run`으로 직접 재현). `CheckPrereqs::apply`가 `ctx.dry_run`을 확인해 그 경로에서 `assumed_prereq_from_labels`를 쓰도록 고쳐 해소했다 — 실 연결(`dry_run == false`) 검증은 그대로 유지된다. **`InstallFleetWorker`**: `target_arch`와 `StepContext.fleet_worker_bin_by_arch`(아키텍처별 로컬 바이너리 경로 맵)를 추가했다. 우선순위는 명시 오버라이드 → 아키텍처 매칭 → 아키텍처 무관 단일 폴백(기존 배포와 완전 하위 호환). dry-run 미리보기 메시지도 같은 선택 로직(`resolve_local_bin`)을 쓰도록 통일했다 — 그러지 않으면 dry-run이 실제 배포와 다른 결과를 보여준다. **이 맵을 CLI/인벤토리 YAML에서 채우는 배선은 `#83` 범위**다. **`InstallCloudflared`**: `target_arch` → `cloudflared_arch_suffix`로 `uname -m`(`aarch64`/`x86_64`) 값을 cloudflared 자산 명명(`arm64`/`amd64`)으로 변환한다. 이전에는 `cloudflared-linux-amd64`가 고정돼 있어 arm64 호스트에서 잘못된 아키텍처 바이너리를 받았다. `#85`에서 표준 playbook 제거가 예정된 과도기 스텝이라 미인식 아키텍처는 하드 실패 대신 경고 후 amd64로 폴백한다. 검증: `crates/fleet-provisioner/src/steps/check_prereqs.rs`(dry-run이 실제 조회 없이 라벨 기반 가정값을 쓰고, 비dry-run은 여전히 fail-closed 검증함을 고정하는 3건 포함 11건), `install_fleet_worker.rs`(아키텍처 매칭·폴백·오버라이드 우선순위·에러 메시지의 arch 명시·dry-run 메시지 일치 5건 포함 11건), `install_cloudflared.rs`(arch suffix 매핑·미인식 폴백·다운로드 URL 선택 4건 포함 10건). `fleet provision --inventory ... --dry-run`을 arm64 라벨 인벤토리로 직접 실행해 회귀가 실제로 해소됐음을 확인 |
| #82 | provision→join 배선 | **완료** | [Worker provisioning](../deployment/worker-provisioning.md), [Enrollment](../contracts/worker-enrollment.md) | `RemoteExecutor::exec_with_stdin`을 신설해(`ssh.rs`, `SshClient`/`MockExecutor` 양쪽 구현) `fleet-worker join --token-file -`에 bootstrap token을 SSH 채널 stdin으로 직접 쓴다(argv·쉘 히스토리·디스크 미경유; `write_file`은 base64를 명령행에 보간하므로 이 목적에는 쓰지 않는다). `InstallFleetWorker` 뒤·`PushCredentials` 앞에 `JoinWorker` 스텝을 신설했다: `POST /v1/bootstrap-tokens`(`max_uses=1`, TTL 기본 300초)로 워커 전용 1회용 토큰을 발급받고, 원격에서 `sudo fleet-worker join --token-file - --orchestrator-url … --name … --config-out /etc/fleet/worker.toml`을 실행한 뒤 `chmod 600`으로 권한만 보정한다. worker.toml 렌더링은 이제 오케스트레이터의 `fleet-api::handlers::render_worker_config_toml` 하나로 단일화됐다 — `templates.rs`의 `render_worker_config`(legacy `[worker] bootstrap_token` 방출, `fleet-worker`가 fail-closed 거부)를 완전히 삭제하고 `TemplateContext`를 cloudflared config.yml 전용 3필드로 축소했다. `steps.rs`(`StepContext.bootstrap_token`)·`inventory.rs`(`ProvisionOptions.bootstrap_token`)·`fleet-cli`(`--bootstrap-token`/`ProvisionArgs.bootstrap_token`)·`fleet-dashboard`(`ProvisionRequest.bootstrap_token`, `provision.html`/`provision.js`의 입력 필드)에서 해당 필드·플래그를 모두 제거했다 — 대시보드 폼은 대신 이제 실제로 소비되는 `api_token`(Orchestrator API Token) 입력으로 교체했다(이전에는 이 필드가 폼에 아예 없어 `PushCredentials`도 이미 조용히 실패하고 있었다). **게이트 재확인**: 로드맵 초안은 `is_applied`가 원격 `GET /v1/workers/{id}` 조회로 신원을 검사해야 한다고 요구했으나, 구현은 로컬 `/etc/fleet/worker.toml`에 `operational_token` 필드가 이미 있는지 grep으로 확인하는 더 단순한 방식을 택했다 — join 성공 시에만 그 필드가 원자적으로 기록되므로 로컬 파일 검사만으로 "이전에 성공적으로 join했다"를 판별하기에 충분하고, `worker:read` 같은 새 capability를 idempotency 검사만을 위해 추가로 요구하지 않아도 된다. 재실행이 필요한 실패 경로(예: join 응답 유실)는 `/v1/workers/join`이 동일 이름에 대해 **재등록을 지원하지 않고 항상 `409 Conflict`로 거부**하는 실제 동작을 그대로 인정해, 조용히 성공 처리하거나 기존 워커를 지우지 않고 운영자가 판단할 수 있는 명확한 에러로 전파한다(원문에 `409`가 있으면 별도 안내 문구를 덧붙인다) — 이것이 "실패 보상은 항상 전진, 워커 삭제로 후퇴하지 않는다" 원칙의 실질적 구현이다. 검증: `crates/fleet-provisioner/src/steps/join_worker.rs`(19건 — prereq 검증 3, dry-run, is_applied 2, 커맨드 구성·quoting 3, 409 판별 2, `perform_join`의 stdin-only 토큰 전달·chmod·에러 매핑 3건 포함), `install_fleet_worker.rs`(worker.toml 미작성 확인으로 갱신), `playbook.rs`/`tests/playbook_dry_run.rs`(8스텝 순서 고정) |
| #83 | 인벤토리 모드 완결성 | **완료** | [Worker provisioning](../deployment/worker-provisioning.md) | `runtime.rs::build_inventory_step_context`의 `fleet_worker_bin: None` 하드코딩을 제거했다 — `InventoryDefaults`에 `fleet_worker_bin`(단일 폴백)/`fleet_worker_bin_by_arch`(아키텍처별 맵, `#81`이 만든 메커니즘을 실제로 채움)를, `InventoryWorker`에 `fleet_worker_bin`(워커별 오버라이드, defaults보다 우선)을 추가했다. `grok_secret`은 "선택 필드로 유지 vs 제거"를 유지로 결정했다 — 타입은 이미 `Option<String>`이었고, `#82`로 미지정 시 원격 `fleet-worker join`이 무작위 생성하므로 필드 자체를 없앨 이유가 없어졌다(재현 가능한 값을 원할 때만 쓰는 선택 사항으로 문서화). `run_provision_inventory`에 `api_token` 필수 검증을 추가했다 — **dry-run이 아닐 때만** 검사한다(dry-run은 `MockExecutor`라 원래도 이 값을 안 보고, `JoinWorker` 자신의 dry-run 분기도 마찬가지라 대칭적이다). 검증 위치는 CLI 병합(`--api-token`이 인벤토리 `options.api_token`을 오버라이드) **이후**다 — `Inventory::validate()`(YAML 자체만 봄)에 두면 `--api-token`을 CLI로 주입하는 기존 운영 패턴(커밋된 인벤토리는 비밀을 담지 않음)을 깨뜨린다. 선언만 있고 참조 0건이던 `retry_failed`는 실제 재시도 로직이 전무했고(반쪽 기능을 새로 설계하는 것은 이 항목의 게이트 범위를 넘어섬) 구현 대신 제거를 택했다 — `ProvisionOptions`와 `examples/workers.yaml`/`config/inventory-from-ssh.yaml`에서 함께 정리했다(`deny_unknown_fields` 미사용이라 기존 YAML의 잔여 필드는 무해하게 무시됨을 회귀 테스트로 고정). 게이트 검증: `./target/debug/fleet provision --inventory config/inventory-from-ssh.yaml --dry-run`을 실제 실행해 25노드 전원이 신설 8단계(`join_worker` 포함) 전체를 완주함을 확인(`25 succeeded, 0 failed`). `api_token` 검증도 별도 실행으로 확인했다 — 토큰 없이 비dry-run 실행 시 SSH 연결 시도 전에 즉시 실패, `--api-token`을 주면 그 검증을 통과해 (예상대로) SSH 단계에서 실패. 신규 테스트: `inventory.rs` 6건(아키텍처 맵 파싱, 워커별 오버라이드 우선순위, 미설정 시 `None`, 제거된 `retry_failed` 필드가 남은 YAML도 계속 파싱됨을 고정하는 하위 호환 테스트 포함) |
| #84 | 원격 파일 전송 방식 교체 | **완료** | 이 행 | `ssh.rs`의 `upload_file`/`write_file`이 base64를 단일 셸 명령행에 보간하던 것(`echo '<b64>' | base64 -d > path`)을 `#82`가 만든 `exec_with_stdin`(SSH 채널 데이터 메시지로 청크 전송 — russh가 내부적으로 `max_packet_size`/window를 지켜 chunking·backpressure를 처리함을 vendored 소스로 확인)로 교체했다. SFTP 서브시스템을 새로 추가하는 대신 이미 있고 이미 검증된 primitive를 재사용했다. 파일 생성 명령 자체에 `umask 077`을 앞세워(`umask 077 && cat > path && chmod {mode}`) 파일이 생성되는 첫 순간부터 `0600`이게 만들고, 필요하면 그 다음에만 목표 mode로 넓힌다 — 좁혔다가 넓히는 순서라 이전처럼 "기본 umask(보통 0644)로 먼저 생성된 뒤 chmod가 뒤따라 붙는" world-readable 창이 없다. `write_file`은 항상 `/tmp` 스테이징 파일에 쓰고 호출자가 별도로 `sudo mv`+`sudo chmod`하는 기존 패턴은 그대로이므로, 이 변경은 그 스테이징 파일 자체가 잠깐이라도 world-readable이 되는 문제만 없앤다. 원격 경로 문자열은 `steps/join_worker.rs`의 `shell_quote`와 동일 로직(인라인 중복, `push_credentials.rs::urlencode`와 같은 관례)으로 quote한다. `MockExecutor`는 애초에 셸을 거치지 않는 synthetic 구현이라 변경 없음 — 기존 테스트가 매칭하는 합성 호출 문자열도 그대로 유지된다. base64 크레이트 의존성을 `fleet-provisioner`에서 완전히 제거했다(다른 용도 0건 확인 후). **게이트 검증(실제 SSH)**: 로컬 macOS `sshd`를 임시 설정(ephemeral host key, 127.0.0.1:2222, 커밋 안 함)으로 띄우고 실제 `SshClient`로 (1) 5MB 랜덤 바이너리 `upload_file`이 성공하고 원격/로컬 바이트가 완전히 동일함(`cmp`)을, (2) `write_file`로 쓴 비밀 콘텐츠가 생성 직후 `stat`으로 확인한 결과 `0600`임(world-readable 아님)을 직접 확인했다 — 검증에 쓴 예시 바이너리(`examples/verify_84.rs`)와 sshd 설정은 검증 후 삭제하고 커밋하지 않았다. 신규 단위 테스트: `ssh.rs`의 `shell_quote_*` 3건(`ssh` feature 게이팅 — `--no-default-features`에서 dead code 경고 나던 것을 잡아 `shell_quote` 자체도 함께 게이팅했다). 검증: `cargo test --workspace`(전체 통과), `DATABASE_URL` 주입 재실행(전체 통과), `cargo check --no-default-features`와 `cargo clippy -p fleet-provisioner --no-default-features` 둘 다 경고 0(후자는 최초 `shell_quote` dead_code 1건 발견·게이팅 후 0), `cargo clippy --all-targets --all-features` 경고 0 |
| #85 | mTLS 자산 배포와 SAN 일관성 | **완료** | [Topology](../deployment/topology.md), [Worker provisioning](../deployment/worker-provisioning.md) | Transport 정본을 mTLS 직접 다이얼로 확정한 데 따른 배포 파이프라인 결손을 닫았다. `IssueMtlsAssets`(신설)를 `InstallFleetWorker` 뒤·`JoinWorker` 앞에, `ConfigureMtls`(신설)를 `JoinWorker` 뒤에 배선해 표준 playbook을 8→9단계로 확장했다. `fleet-cli`가 CLI/인벤토리 레벨에서 이미 재사용 가능한 형태였던 `mtls::run_issue_server`(리팩터링 불필요 — 처음부터 `pub fn ... -> Result<()>`였다)를 매 워커마다 로컬 임시 디렉토리에 직접 호출해 서버 인증서를 발급한다(SAN=`mtls_advertised_host`) — orchestrator API나 `fleet-provisioner`는 rcgen/서명 로직을 전혀 모른다. `IssueMtlsAssets`는 그 로컬 3파일을 `/tmp` 스테이징 + `sudo mv`로 원격 `/etc/fleet/mtls/{server.pem,server.key,ca.pem}`(고정 경로)에 업로드한다(`upload_file` 대신 이 패턴을 쓴 이유: `upload_file`은 SSH 로그인 사용자 권한으로 직접 쓰는데 `/etc/fleet/mtls`는 `sudo mkdir -p`로 root 소유가 되므로). `JoinWorker`는 mTLS 활성화 시 `grok_secret` 미지정이면 여기서 직접 생성해 `--grok-secret`과 `--agent-endpoint wss://{advertised_host}:{advertised_port}/ws?server-key={secret}` 양쪽에 동일하게 넘긴다(그러지 않으면 `fleet-worker join`이 리버스 SSH 터널 시절 형태로 자동 유도한다). `ConfigureMtls`는 `fleet-api`가 `[mtls]`를 전혀 모르는 join 응답 스키마 결손(`JoinRequest`/`JoinResponse`/`render_worker_config_toml` grep 0건, 구현 전 조사로 확인)을 오케스트레이터 스키마 확장 대신 `push_credentials.rs`와 같은 원격 read-append-atomic write로 우회한다 — `[worker]` 섹션 존재를 sanity check해 `--tags mtls`로 `JoinWorker` 없이 단독 실행되는 경로를 조용히 깨진 파일로 만들지 않는다. `advertised_host`가 SAN과 항상 같은 값이 되는 것은 별도 검증 코드가 아니라 두 값의 출처(`ctx.mtls_advertised_host`)가 애초에 하나라는 구조로 보장한다. `InventoryDefaults.mtls_client_ca`/`InventoryWorker.mtls_server_cert`/`mtls_server_key`(전부 참조 0건이던 옛 "경로만 채운다" 모델)를 제거하고 `ProvisionOptions.mtls_ca_dir`(fleet 전체가 공유하는 로컬 CA 디렉토리, `api_token`과 같은 패턴)로 대체했다. 표준 playbook에서 `InstallCloudflared`를 제거했다(타입 자체는 남겨 커스텀 playbook에서 여전히 구성 가능) — `topology.md`가 Cloudflare Tunnel을 지원 대상에서 제외했고, 조사 중 `sudo systemctl enable cloudflared 2>&1 \|\| true`가 설치 실패를 무조건 exit 0으로 만들어 뒤이은 실패 검사를 실질적으로 죽은 코드로 만드는 것도 확인했다. **게이트 검증**: (1) 인증서 없이 mTLS 활성화 — 단일 호스트/인벤토리 모드 둘 다 SSH를 하나도 시도하기 전에 명확한 에러로 실패함을 실제 CLI 실행으로 확인. (2) SAN 불일치 감지 — `fleet mtls init-ca`/`issue-server`/`issue-client`(실제 CLI)로 발급한 인증서를 `fleet-transport`의 실제 `ServerTlsConfig`/`ClientTlsConfig`/`MtlsProxy`(`runner.rs`/`acp_transport.rs`가 쓰는 바로 그 타입)로 로드해 진짜 TLS handshake를 실행하는 임시 예제로, 틀린 SNI가 `rustls`의 `InvalidCertificate(NotValidForNameContext)`로 정확히 거부됨을 확인(커밋하지 않음, 검증 후 삭제) — SAN 일관성은 별도 감지 로직이 아니라 TLS 자체의 고유 동작이자 자동 발급이 애초에 불일치를 만들 수 없는 설계로 보장됨을 함께 확인. (3) 같은 임시 예제로 올바른 SAN 사용 시 실제 TCP+TLS handshake가 성공하고 페이로드가 `MtlsProxy` 경유로 정확히 왕복함을 확인 — ACP JSON-RPC 레이어 자체(`acp_transport_mtls.rs`)는 이미 별도로 테스트돼 있어 여기서 재검증하지 않았다. (4) 커밋된 25노드 인벤토리(전원 mTLS 비활성)로 `--dry-run`을 재실행해 신설 9단계 전체가 여전히 완주함을 확인(`25 succeeded, 0 failed`). 신규 테스트: `issue_mtls_assets.rs` 6건, `configure_mtls.rs` 9건, `join_worker.rs`에 mTLS agent-endpoint/secret 관련 7건 추가, `inventory.rs` mTLS 스키마 갱신 관련 2건, `playbook.rs`/`tests/playbook_dry_run.rs` 9단계 순서 고정. 검증: `cargo test --workspace`(전체 통과), `DATABASE_URL` 주입 재실행(전체 통과), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features`와 `-p fleet-provisioner -p fleet-cli --no-default-features`(mtls feature 꺼진 빌드) 둘 다 경고 0 |

### UI 관리 표면과 Issue 추적 (#86-#93)

2026-08-22 [UI 관리 대상·Issue 명세 설계](../reviews/ui-management-and-issue-spec-2026-08-22.md)에서
도출했다. host·worker·task UI는 이미 구현됐고 project·agent UI는 `#48`·`#49`가 소유하므로 여기서
중복 등록하지 않는다. 신규는 agent_template 정본화와 Issue 추적 둘이다.

**2026-08-23 프레임 정정**: Issue는 orchestrator의 인프라 장애 추적이 아니라 **프로젝트가 해결해야
할 일감을 관리하는 이슈 트래커**다. 이에 따라 dead-letter 자동 Issue 생성 항목을 취소하고 관측
요구를 `#70`으로 옮겼으며, Agent가 백로그에서 일을 집어가는 경로(`#93`)를 추가했다.

**하드 순서 제약**: `#86` catalog 부분(global 템플릿)은 `#48` 전에도 성립하나 project-scoped
템플릿은 `#48` 뒤다. `#88`은 Issue가 정의상 Project scope 자원이라 `#48` 없이 착수할 수 없다.
두 모듈 모두 `#73` 전에 `/v1` route를 추가하면 안 된다 — 행렬 미등록 route가 통과하는 창이 열린다.

| ID | 항목 | 우선순위·상태 | 정본 | 완료 게이트 |
|---|---|---|---|---|
| #86 | AgentTemplate 정본과 revision immutability | P2 · 설계 확정·구현 대기 | [AgentTemplate](../architecture/agents/agent-template.md) | `agent_templates`(정체성)와 `agent_template_revisions`(불변 본문) 2계층. `Draft/Published/Deprecated/Retired/Discarded` 전이와 `Retired → Published` 간선 부재. published revision의 content 변경 거절, 같은 content 재발행이 새 revision id에 같은 `content_hash`. retire가 dependent set 해시 없이 실패하고 집합 변화 시 409. 템플릿이 Project grant를 넘는 tool을 부여하지 못하며 저장 후 grant가 좁아진 경우에도 admission에서 차단. `builtin/default@1` 시드가 두 store에서 같은 `content_hash`이고 tool binding이 `ReadOnly` 등급 한정. MemStore/PgStore 공유 행동 테스트. **권한(2026-08-22 결정)**: 편집은 필드별로 게이팅한다 — `role_prompt`·메타데이터는 `agent_template:update`, `tools`/`skills`/`isolation_class`는 거기에 Agent tool-binding 권한(`AgentManage` 상당)을 추가 요구. `agent_template:update`만 가진 principal의 tool 필드 변경이 403인 시험과, operator 역할(`read`+`update` 부여)이 실질적으로 prompt만 편집 가능함을 역할 번들 기준으로 검증. `#48`의 구현 차단 조건은 이 항목에 적용하지 않는다 — 그 차단은 자동 provisioning을 통한 `AgentCreate` 우회를 겨냥하며 템플릿 편집은 Agent를 만들지 않는다 |
| #87 | Attempt snapshot의 template revision 고정 | P2 · 설계 확정·구현 대기 | [AgentTemplate](../architecture/agents/agent-template.md), [실행 일관성](../architecture/tasks/execution-consistency.md) | 선행 `#62`, `#86`. Attempt는 revision을 참조하지 않고 본문·hash를 materialize한다. 실행 중 revision retire가 Attempt harness manifest hash를 바꾸지 않는 E2E. WarmIdle 호환성 키에 `agent_template_revision_id`가 포함되어 불일치 시 `WarmIdle → Hibernated → Starting`만 발생. `Retired` pin의 `Hibernated → Starting`이 신설 `FailureKind::TemplateUnavailable`로 admission 즉시 거절되고 fallback도 retry 예산 소모도 없음. `Deprecated`는 기동을 막지 않고 경고 metric만 |
| #88 | Issue 엔티티와 상태 머신 | **완료 (2026-08-24)** | [Issue 추적](../architecture/issues.md) | 선행 `#48`, `#73`, `#76`. `Open/Triaged/ReadyForAgent/Resolved/Closed(+reason)`이며 `InProgress` 부재(비터미널 연관 Task에서 유도). `tasks`에 `issue_id` 컬럼을 두지 않고 join 테이블로 연관. **교착 없음 3종**: 열린 Issue가 있어도 Task가 dead-letter까지 도달, 비터미널 Task가 있어도 Issue close 성공, Attempt 전이 코드 경로가 issue 테이블을 참조하지 않음을 강제하는 구조 시험. MemStore/PgStore 공유 행동 테스트 **구현(이번)**: 선행 `#48`(1·2단계)·`#73`·`#76`이 모두 충족돼 착수했다. `crates/fleet-core/src/issue.rs`(도메인 + 상태 머신), migration `023_issues.sql`, `Store` Issue 메서드 10종(PgStore+MemStore), `PermissionKind` 9종. **`tasks`에 `issue_id` 컬럼을 두지 않았다** — 연관은 `issue_task_links` join 테이블이 소유하며, `task_id`는 `SET NULL` + `task_label` 보존(`011`의 `actor_label` 패턴)이다. `InProgress` 부재를 세 겹으로 고정했다: enum에 없음, `IssueStatus::ALL` 순회 테스트, DB CHECK 제약(`in_progress` INSERT가 거절되는 것을 실제 Postgres로 확인). `close_reason`은 `status='closed'`일 때만·그때는 반드시 있어야 한다는 불변식을 `Issue::transition_to`와 DB CHECK 양쪽에 뒀다 — 애플리케이션을 우회하는 쓰기도 막기 위함. `issue:update`(오탈자)와 `issue:close`(종결)를 분리한 계약을 저장소 API에서도 유지해 `update_issue_fields`는 status를 건드리지 못한다(로컬 사본의 status를 조작해 호출해도 저장된 상태가 안 바뀌는 것을 테스트로 고정). **의도적으로 미구현**: Agent가 여는 Issue(dedup key/`occurrence_count`/`origin_attempt_id`)는 `#89`(Worker control stream + `#67` 선행), backlog claim은 `#93`, HTTP·MCP 표면은 `#92`, `issue:archive_hold_manage`는 대상 테이블이 없어 `#91` — 전부 채울 방법이 없는 것을 미리 만들지 않는다는 `#48`/`#70`의 판단을 그대로 적용했다. 신규 테스트: `fleet-core::issue` 13건(전이표 전체를 훑어 허용 간선 집합이 계약 다이어그램과 정확히 일치하는지 확인하는 것 포함), `crates/fleet-store/tests/issues.rs` 14건 — 11건은 `both_backends!` 매크로로 MemStore와 PgStore **양쪽에** 같은 시나리오를 돌려 게이트 10을 만족하고, 3건은 DB CHECK 제약 전용. 교착 없음(게이트 2)은 두 방향 모두 실증했다: `ReadyForAgent` Issue에 연관된 Task가 터미널까지 도달하고, 비터미널 Task가 있어도 Issue close가 성공하며 그 Task가 살아남는다. 검증: `cargo test --workspace`(99 스위트 전체 `ok`), `DATABASE_URL` 주입 재실행(사전 존재 버그 1건 제외 전체 통과), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0 |
| #89 | Agent 보고 경로와 폭주 방지 | P2 · 설계 확정·구현 대기 | [Issue 추적](../architecture/issues.md), [Agent provisioning](../architecture/agents/provisioning.md) | 선행 `#88`, `#67`. Agent는 principal이 없으므로 Worker control stream 보고 → control plane 대리 생성. `project_id`를 요청 본문이 아니라 저장된 Attempt 행에서 유도(본문 위조가 무시되는 시험). 부분 유니크 인덱스로 동일 blocker N회가 Issue 1건 + `occurrence_count=N`. Attempt당 상한 초과가 Attempt를 실패시키지 않음. Project 버킷 소진 시 `AgentIssueFloodSuspected` alert이고 dedup 적중은 토큰 미소모. metric label에 UUID·prompt 미노출. 감사 실패 시 Issue 생성 거절 |
| #90 | ~~dead-letter → Issue 집계 자동 생성~~ | **취소 (2026-08-23)** | [관측성·재조정](../architecture/observability-and-reconciliation.md) | Issue를 이슈 트래커로 재정의하면서 취소했다 — 인프라 장애는 alert이지 프로젝트 일감이 아니다. 조사 중 확인된 사실: `crates/fleet-scheduler/src/reconcile.rs`의 dead-letter 경로는 `CredentialMissing`과 `WorkerUnavailable` 두 kind만 붙이고, `FailureKind`의 `Timeout`·`AuthFailed`·`Cancelled` 세 variant는 **저장소 어디서도 생성되지 않는다**(죽은 코드). `/metrics`에도 kind별 분해가 없어 운영자가 실패 원인을 집계할 수단이 없다. 이 관측 요구는 `#70`이 흡수한다. ID는 참조 안정성을 위해 재사용하지 않는다 |
| #91 | Issue → archive hold 승격과 drain 순서 | P2 · 설계 확정·구현 대기 | [Issue 추적](../architecture/issues.md), [Lifecycle](../architecture/project-task-agent-lifecycle.md) | 선행 `#68`, `#88`. 열린 Issue만으로는 archive가 막히지 않음. `kind='issue'` hold가 `ArchiveBlocked`를 만들고 기존 hold 해제 경로로만 풀림. 승격에 `issue:update`가 아니라 Project hold capability 필요. `Draining` 중 Issue 쓰기는 허용하고 Issue→Task 생성만 차단. archive 후 read-only 봉인, Project reopen이 Issue를 자동 reopen하지 않음 |
| #92 | AgentTemplate·Issue 관리 표면 노출 | P2 · 설계 확정·구현 대기 (Issue 표면 HTTP·MCP 완료 2026-08-24) | [Authorization](../security/authorization-and-audit.md) | 선행 `#73`(Dashboard 표면 포함), `#86`, `#88`. `agent_template:{read,create,update,archive,revision:revoke,manage_global}`와 `issue:{read,create,comment,update,assign,close,reopen,link,archive_hold_manage}` 신설 — UI 문서의 단일 `AgentTemplateManage`는 채택하지 않고 `#66`·`#72`의 분리 선례를 따른다. 모든 신규 route의 행렬 등록을 강제하는 커버리지 테스트. Project scope 밖 조회는 404, scope 안 권한 부족은 403. `ApiError`에 422/428/429가 없어 낙관적 동시성(`If-Match` → 409/428)과 rate limit(429)을 표현할 수 없으므로 확장이 선행된다. `BuiltinRole::Operator` 고정 목록에 `agent_template:read`/`agent_template:update`를 추가한다(admin은 `PermissionKind::all()`로 자동 보유하며 `builtin_roles_cover_all_permissions`가 강제) **Issue 표면 완료(이번)**: 이 항목은 서로 독립인 두 도메인(AgentTemplate, Issue)을 "관리 표면"이라는 주제로만 묶은 것이라, 선행이 충족된 쪽부터 갈라 진행했다 — Issue 쪽은 `#73`·`#88`이 모두 완료됐고 AgentTemplate 쪽만 `#86`에 막혀 있다. `ApiError`의 422/428/429 확장은 필요 없었다 — 그 요구는 AgentTemplate revision의 낙관적 동시성(`If-Match`)에서 오며 Issue 표면은 400/403/404/409로 충분하다(기존 `ApiError`가 넷 다 갖고 있다). 노출한 것: Dashboard `GET/POST /api/issues`, `GET/PATCH /api/issues/{id}`, `POST /api/issues/{id}/transition`, `GET/POST /api/issues/{id}/comments`, `GET/POST /api/issues/{id}/links`, `DELETE /api/issues/{id}/links/{task_id}`. **상태 전이를 `PATCH`와 분리한 것이 이 표면의 핵심 설계다** — 목표 상태마다 요구 capability가 다르기 때문이며(`handlers::required_capability_for_transition`), 저장소 계층에서 이미 `update_issue_fields`/`transition_issue`로 갈라 둔 것과 같은 이유다. 전이별 매핑과 그 근거: `→ReadyForAgent`는 `issue:approve_agent_work`(계약이 "Agent 자동 착수의 유일한 인가 지점"으로 못 박은 전이), **`ReadyForAgent→Triaged`(승인 철회)는 그 capability를 요구하지 않는다** — 계약이 approve를 한 방향으로만 정의했고, 권한 회수가 부여보다 어려우면 잘못된 승인을 되돌리기가 더 힘들어져 안전한 방향으로 실패하지 않는다; `→Resolved`와 `→Closed`는 둘 다 "이 문제가 처리됐다"는 판정이므로 `issue:close`(계약이 close를 update에서 분리한 이유가 "오탈자 수정 권한이 문제 종결 권한을 함께 주면 안 된다"인데, Resolved도 텍스트 편집이 아니라 문제 상태에 대한 판정이다); `→Open`은 `issue:reopen`; `→Triaged`(severity·labels·owner 지정)는 편집 성격이라 `issue:update`. `assignee` 변경만 `issue:assign`을 **추가로** 요구한다(다른 필드 수정에 까지 요구하면 계약의 capability 분리가 무의미해진다). `has_active_tasks`는 저장하지 않고 조회 시점에 계산해 응답에 싣는다 — `InProgress` 상태를 두지 않은 이유가 그것이다. `Draining` Project의 Issue 쓰기는 허용한다(계약: "`Draining` 중에도 Issue 쓰기는 허용하고 claim과 Issue→Task 생성만 막는다") — 생성 시 Project 존재만 확인하고 상태는 보지 않는다. 신규 감사 액션 `issue.create`/`issue.transition`(전이는 `detail.to`에 목표 상태를 남긴다 — `ready_for_agent` 승인이 누구였는지가 감사에 남아야 한다). 신규 테스트 16건: 전이별 capability 매핑을 계약과 대조하는 단위 테스트(승인 철회가 approve를 요구하지 **않음**과 Resolved/Closed가 update로 안 됨을 `assert_ne!`로 명시), 전체 lifecycle 왕복, 허용되지 않는 간선 409, 사유 없는 close 409, `issue:update`만으로 승인·종결 불가 403(저장된 상태가 안 바뀌는 것까지 확인), PATCH가 본문에 status를 끼워 넣어도 상태를 못 바꿈, assignee만 추가 권한, 코멘트 왕복, Task 연관이 파생 배지를 켜되 status는 그대로, read 권한·CSRF 강제. **남은 것**: AgentTemplate 표면(`#86` 선행), Issue의 MCP 표면(설계상 Issue의 모든 전이가 사람의 행위이고 Agent/Worker에겐 어떤 issue capability도 부여하지 않으므로 Dashboard보다 우선순위가 낮다 — 다만 계약의 "두 표면 동일 동작" 요구는 남아 있어 `required_capability_for_transition`을 pub으로 노출해 재사용 가능하게 뒀다), `issue:archive_hold_manage`(`#91` 선행). **MCP 표면 추가(이어서)**: `fleet_list_issues`/`fleet_create_issue`/`fleet_transition_issue`/`fleet_comment_issue`. 이 작업에서 `#92` HTTP 표면 때 만든 `required_capability_for_transition`의 **위치를 바로잡았다** — `fleet-dashboard`에 뒀는데 `fleet-mcp`가 그 크레이트에 의존하지 않아 공유가 불가능했다. `IssueStatus`와 `PermissionKind` 둘 다 `fleet-core`에 있고 Store 조회가 필요 없는 순수 함수이므로 `fleet-core`로 옮겼고, 두 표면이 같은 구현을 참조하게 돼 계약의 "두 표면 동일 동작"이 구조적으로 보장된다(surface 크레이트에 공유 도메인 정책을 두는 것이 애초에 층위 오류였다). **인자 의존 인가 문제**: `fleet_transition_issue`는 요구 capability가 목표 상태에 따라 달라 MCP 서버의 도구-이름 capability 행렬로 판정할 수 없다. 두 단계로 나눴다 — `permits_tool`은 "전이 권한을 하나라도 가졌는가"로 도구 **노출**만 결정하고(하나도 없으면 `tools/list`에 나오지 않는다), 정확한 판정은 핸들러가 `ToolContext.capabilities`로 한다. `ToolContext.capabilities`의 기본값은 빈 집합(fail-closed)이라 명시 부여 없이는 인자 의존 도구가 전부 거절된다. 신규 테스트 6건: 생성·목록·코멘트 왕복, 미존재 project 거절, 상태 기계 준수(허용 안 되는 간선·사유 없는 close 거절), **전이별 capability 판정**(`issue:update`만 가진 launcher가 triage는 되고 승인·종결은 거절되며 저장 상태도 안 바뀜, 거절 메시지가 없는 capability를 명시), 미존재 issue 처리, 그리고 전이 권한이 하나도 없으면 도구 자체가 노출되지 않음. 검증: `cargo test --workspace`(99 스위트 전체 `ok`), `DATABASE_URL` 주입 재실행(사전 존재 버그 1건 제외 전체 통과), `cargo check --no-default-features`, `cargo clippy --all-targets --all-features` 경고 0 |
| #93 | Agent backlog claim (Issue 자동 착수) | P2 · 설계 확정·구현 대기 | [Issue 추적](../architecture/issues.md) | 선행 `#88`, `#89`, `#48`. Agent가 `ReadyForAgent` Issue를 집어 Task를 만든다. **승인은 상태가 소유한다** — `Triaged → ReadyForAgent` 전이는 사람만 하며 신설 `issue:approve_agent_work` capability를 요구한다. claim은 `(issue_id, status, claim_generation)` CAS이고 만료되는 lease를 얻으며, 만료 시 `ReadyForAgent`로 복귀한다(`#62`의 lease 관례 재사용, 새 기구 금지). claim은 Issue 상태를 바꾸지 않는다 — 상태를 하나 더 만들면 `InProgress`를 금지한 것과 같은 문제가 생긴다. Project는 동시 claim 수와 시간당 claim 수를 정책으로 갖는다 — 예산이 없으면 무한 생성-소비 루프가 성립한다. 예산 소진은 실패가 아니라 대기이며 지속되면 alert. **선행 게이트(2026-08-25 추가)**: 착수 전에 MCP stdio launcher의 `FLEET_MCP_CAPABILITIES`에서 `issue:approve_agent_work`를 재검토한다 — 2026-08-25 운영자 결정으로 전체 개방돼 있고, 그 표면에는 호출 principal이 없어 LLM이 승인 전이를 수행할 수 있다. `#93` 미구현 동안에는 `ReadyForAgent`가 표식에 그쳐 무해하지만 이 항목이 구현되는 순간 그 전제가 무너지므로, 구현 착수와 동시에 (a) 해당 표면에서 이 capability를 제거하거나 (b) MCP `ToolContext`에 호출 principal을 도입해야 한다. 게이트: `ReadyForAgent`가 아닌 Issue는 claim되지 않음, 사람 승인 전이 없이 Agent가 자기가 연 Issue를 착수할 수 없음, claim CAS 경쟁에서 정확히 하나만 성공, lease 만료 복귀, `origin_issue_id` 계보 깊이 상한이 순환을 끊음, `Draining` 중 claim 거절 |

## 보존 항목 레지스트리 (#1-#47)

완료·강등·폐기된 ID는 참조 안정성을 위해 재사용하지 않는다. 상세 변경 경위는 Git 이력과
관련 코드·문서에서 확인한다.

| ID | 항목 | 종결 상태 | 대표 근거 |
|---|---|---|---|
| #1 | Dockerfile | 완료 | `acca872` |
| #2 | docker-compose | 완료 | `acca872` |
| #3 | password reset rate limit | 완료 | `b501ca5` |
| #4 | PgStore auth integration test | 완료 | `afd8d35` |
| #5 | latency·throughput metrics | 완료 | `b501ca5`, `8755c0d` |
| #6 | DB backup script | 완료 | `853a1d0` |
| #7 | restrictive CORS | 완료 | `1d4422e` |
| #8 | fleet-api security headers | 완료 | `1d4422e` |
| #9 | structured audit log | 완료 | `b501ca5` |
| #10 | session token rotation | 완료 | `0177e56` |
| #11 | pagination·label filter propagation | 완료 | `7e17558` |
| #12 | API error envelope | 완료 | `8755c0d` |
| #13 | OpenTelemetry tracing | 완료 | 코드·테스트 |
| #14 | dark mode·sort·filter | 완료 | 코드·테스트 |
| #15 | startup configuration validation | 완료 | `0177e56` |
| #16 | connection pool tuning | 완료 | `bac4dc7` |
| #17 | `fleet_list_tasks` MCP tool | 완료 | `3fb296a` |
| #18 | scheduled DB cleanup | 완료 | `bac4dc7` |
| #19 | migration rollback tooling | 완료 | `853a1d0` |
| #20 | constant-time bearer comparison | 완료 | `1d4422e` |
| #21 | fleet-api OpenAPI | 완료 | 코드·테스트 |
| #22 | Dashboard API version prefix | 강등 | first-party bundled client 전용 |
| #23 | task list pagination UI | 완료 | 코드·테스트 |
| #24 | mobile responsive audit | 완료 | UI 검증 기록 |
| #25 | shared circuit-breaker state | 완료 | 코드·통합 테스트 |
| #26 | external secret manager | 강등 | 현재 배포 범위 밖 |
| #27 | example configuration | 사실상 충족 | `examples/`, deployment docs |
| #28 | token·host·breaker MCP tools | 완료 | 코드·테스트 |
| #29 | dynamic Dashboard tool list | 완료 | `8755c0d` |
| #30 | CI coverage report | 완료 | `afd8d35` |
| #31 | dispatch latency metric | 완료 | `ed82b27` |
| #32 | `/admin/*` HTML RBAC | 완료 | `db614ec` |
| #33 | security findings S1-S6 | 완료 | [보안 발견 이력](../security/reports/security-findings.md) |
| #34 | liteLLM 기본 연동 | 완료·후속 #53-#56 | [LLM Gateway](../architecture/llm-gateway.md) |
| #35 | Mermaid migration·docs consistency | 완료 | Git 이력 |
| #36 | mTLS certificate auto-rotation policy | 완료 | deployment·security docs |
| #37 | inventory-based mTLS provisioning | 완료 | code·runbook |
| #38 | scheduler retry·DLQ | 완료 | code·tests |
| #39 | known-hosts TOFU deployment gap | 완료 | `fleet scan-host-keys` |
| #40 | `xai-circuit-breaker` adoption | 재평가·종결 | 현 구현 유지 |
| #41 | WebSocket demuxer concurrency | 완료 | code·tests |
| #42 | distributed OTLP propagation | 완료 | code·tests |
| #43 | autonomic self-healing engine | 폐기 | Git 이력; ID 재사용 금지 |
| #44 | HalfOpen single-probe enforcement | 완료 | code·tests |
| #45 | duplicated `MemStore` consolidation | 완료 | code·tests |
| #46 | docs fact-check·structure cleanup | 완료 | docs·Git 이력 |
| #47 | HealthChecker↔Task integration | 완료 | code·tests |

## 갱신 시점

- 요구 승인 시 ID, 독자 가치, 우선순위와 책임 문서를 등록한다.
- 설계 승인 시 정본 링크, 구현 단계, 선행 조건과 검증 가능한 완료 게이트를 확정한다.
- 구현 착수 시 `구현 중`, 코드·테스트·운영 문서가 게이트를 충족하면 `완료`로 바꾼다.
- 계약이나 범위 변경은 설계 정본을 먼저 수정한 뒤 이 문서의 순서·게이트만 동기화한다.
- 폐기하거나 강등해도 ID는 삭제·재사용하지 않고 한 줄 종결 기록을 남긴다.

특정 시점의 테스트 개수, 상세 회고와 설계 개정 횟수는 누적하지 않는다. 현재 사실은 CI와 코드,
변경 경위는 Git 이력과 필요한 경우 `docs/reviews/`가 담당한다.
