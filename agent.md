# 에이전트 협업 가이드 (Agent Collaboration Guide)

> 작성일: 2026-08-06. 최종 개정: 2026-08-17 — 문서 정책 전문을 Governance 정본으로 분리.
>
> 이 문서는 Grok Fleet Orchestrator 프로젝트에 기여하는 모든 AI 개발 에이전트(Agent)가 개발 과정에서 반드시 지켜야 할 **Git 정책, 개발 시퀀스, 보안baseline 및 테스트 검증 규칙**을 명시합니다. 에이전트는 기여를 시작하기 전에 본 가이드를 완전히 숙지해야 합니다.

---

## 1. Git 정책 (Git Policy)

에이전트는 프로젝트의 변경 이력을 명확히 하고, 코드의 추적성(Traceability)을 확보하기 위해 다음 Git 정책을 철저히 이행해야 합니다.

### 1.1 Conventional Commits 표준 준수
모든 커밋 메시지는 다음 접두사(Prefix) 규칙을 준수하여 작성해야 합니다.

*   `feat:`: 새로운 기능 구현 (예: `feat: add database persistence for circuit breaker`)
*   `fix:`: 버그 수정 (예: `fix: cloudflare jwt verification issue`)
*   `test:`: 테스트 코드 추가 또는 수정 (예: `test: add scale-out sync integration test`)
*   `docs:`: 문서 수정 또는 생성 (예: `docs: update example domains to agentthread.dev`)
*   `refactor:`: 기능 동작은 같으나 코드 구조를 리팩토링하는 경우

### 1.2 로드맵 번호 매핑 및 이력 추적성
*   **로드맵 매핑**: [구현 로드맵](docs/roadmap/roadmap.md)에 등록된 작업이라면 커밋 본문이나 제목에 영구 ID(`#N`)를 명시하여 변경 사유를 증적화합니다.
    *   *예시*: `fix: reject incomplete LLM proxy configuration (#53)`
*   **Git Log 보존**: 로컬 Git Log는 메인 브랜치의 신뢰 원천입니다. 변경 전후의 히스토리가 꼬이지 않도록 이전에 정상 작동하던 커밋을 훼손하지 않습니다.

### 1.3 무결성 커밋 단계
1.  **Stage (`git add`)**: 변경된 소스 및 관련 문서 파일만 정밀하게 추가합니다. 개인/로컬 전용 파일(`.claude/settings.local.json`, `.claude/worktrees/` 등)이 스테이징 영역에 포함되지 않도록 마스킹 처리합니다. `.claude/settings.json`, `.claude/hooks/`, 팀 공유 `.claude/agent-memory/` 항목처럼 저장소에 이미 커밋되어 팀 전체가 공유하는 자동화·지식은 임시 파일이 아니므로 이 규칙의 대상이 아닙니다.
2.  **Commit (`git commit`)**: 상세한 변경 내역(변경 부분, 이유, 연동된 로드맵 이슈 번호)을 본문에 포함하여 커밋합니다.

---

## 2. 개발 로드맵 (Development Roadmap)

현재 구현 순서, 영구 ID, 상태와 완료 조건은 [Roadmap 도메인](docs/roadmap/README.md)이
소유합니다. 에이전트는 작업 전 관련 정본 설계와 Roadmap 완료 게이트를 함께 확인하고,
범위가 바뀌면 설계 정본을 먼저 갱신한 뒤 Roadmap의 순서·상태만 동기화합니다.

---

## 3. 보안 및 품질 검증 기준

에이전트는 코드를 병합하기 전에 반드시 로컬 하네스를 가동하여 완전성을 자체 증명해야 합니다.

1.  **Real IP 헤더 무결성**:
    모든 프록시 헤더를 신뢰하기 전에 `FLEET_TRUSTED_PROXIES` allow-list에 선언된 신뢰 프록시 대역인지 1차로 필터링해야 하며, 임의의 헤더 주입으로 인한 IP 위조 공격(IP Spoofing)에 대응할 수 있도록 아키텍처를 견고하게 구현합니다.
2.  **E2E 단위 및 통합 테스트**:
    *   에이전트는 수정 완료 후 반드시 `cargo test`를 실행하여 컴파일 경고나 테스트 실패가 없음을 확인해야 합니다.
    *   **`DATABASE_URL`을 주입한 통합 테스트는 변경 종류와 무관하게 매번 돌립니다.** `fleet-store` 통합
        테스트와 스케일아웃 동기화 테스트(`scaleout_sync`)를 직렬 가동합니다. 예전에는 "SQL 쿼리가
        수정되었거나 DB 트레이트가 변경되었을 경우"라는 조건이 붙어 있었고, 그 조건이 2026-08-26의
        CI 실패 4건을 로컬에서 감췄습니다 — **발동 조건을 변경 종류로 좁게 읽으면, 내 변경과 무관하게
        이미 깨져 있던 것은 영원히 보이지 않습니다.** CI는 모든 잡에 postgres 서비스를 붙여 조건 없이
        돌리므로, 조건부 게이트는 그 자체로 CI보다 약합니다(§4.3의 재발 법칙).
    *   **`cargo test` 앞에 `fleet` 바이너리를 그 잡의 피처로 먼저 빌드합니다.** 축약하지 말고 그대로 실행합니다:
        ```bash
        cargo build -p fleet-cli --features "acp mtls"      # acp+mtls 세트로 테스트할 때
        cargo build -p fleet-cli --no-default-features      # 최소 기능 세트로 테스트할 때
        ```
        `fleet-mcp`의 `cross_client` 테스트는 `target/debug/fleet`를 subprocess로 띄우는데, 이 경로는
        Cargo 의존성 그래프에 잡히지 않는 **런타임 전용 참조**입니다. `fleet-cli`에 통합 테스트가 없어서
        `cargo test --workspace`는 `src/main.rs`를 test 타깃으로만 컴파일하며 — **이 바이너리를 만들지
        않습니다**(2026-08-26 실측: `rm -f target/debug/fleet` 후 `cargo test --workspace --no-run`을
        완주해도 파일이 재생성되지 않음). 빌드를 빠뜨리면 바이너리가 **낡았을** 때는 테스트가 실패하고,
        **없을** 때는 하네스가 시끄럽게 panic합니다(예전에는 조용히 skip했고, 그래서 CI가 이 파일을
        한 건도 실행하지 않은 채 초록을 보고했습니다 — §4.3의 (3) 사례).
    *   **`cargo test`에 `-- --test-threads=1`을 붙입니다.** CI(`ci.yml`)가 두 잡 모두에서 쓰는
        형태이므로 축약하지 말고 그대로 실행합니다:
        ```bash
        DATABASE_URL=postgres://$(whoami)@localhost/fleet_test \
            cargo test --workspace --features "acp mtls" -- --test-threads=1
        DATABASE_URL=postgres://$(whoami)@localhost/fleet_test \
            cargo test --workspace --no-default-features -- --test-threads=1
        ```
        `fleet-store` 통합 테스트 14개 파일과 `fleet-scheduler`의 `scaleout_sync`는 각 테스트가
        시작할 때 `TRUNCATE ... CASCADE`로 테이블을 비우는 방식으로 격리합니다. 이 격리는 **직렬
        실행을 전제로만 성립**합니다 — 병렬로 돌면 한 테스트의 TRUNCATE가 다른 테스트가 방금 넣은
        행을 지웁니다. 위 bullet 2의 "직렬 가동"은 크레이트를 하나씩 돌리라는 뜻이 아니라 **한
        바이너리 안에서의** 직렬성을 말합니다.

        **여기서 로컬 게이트는 CI보다 약한 게 아니라 엄격합니다.** 병렬은 직렬보다 강한 조건이고,
        그런데도 결과는 같습니다 — 판정이 CI의 판정이 아니게 됩니다. §4.3이 "게이트가 CI보다
        약하면 재발한다"로 적어 온 법칙의 일반형은 이것입니다: **게이트와 CI가 다르면 어느 쪽이
        엄격하든 로컬의 판정은 CI의 판정이 아니다.**

        2026-08-28에 이 일이 **하루에 두 번** 일어났습니다. 오전에 `#49`의 `tests/agents.rs`가
        병렬에서 10건 중 6건 실패했고, 원인 규명과 위 결론까지 전부 `docs/log.md`에 기록됐습니다.
        그런데 그 기록이 **이 게이트 목록으로 옮겨지지 않아서**, 같은 날 오후 `#69` 게이트에서
        플래그를 다시 빠뜨렸고 70개 suite 중 10개, 78건이 실패했습니다. 재발의 원인은 새로운
        무지가 아니라 **서술만 갱신하고 실행 목록을 갱신하지 않은 것**입니다 — `docs/log.md`는
        무슨 일이 있었는지를 남기고, 이 파일은 다음에 무엇을 실행할지를 정합니다. 다음 세션의
        에이전트는 로그가 아니라 이 목록을 보고 명령을 만듭니다.

        **실패의 모양이 원인을 가릅니다.** 코드 결함이면 특정 단정이 일관되게 깨지지만, 오염이면
        `left`가 매번 다른 값(9·11·13·2)으로 나오고 `audit_log_actor_user_id_fkey` 위반처럼
        테스트가 **의도하지도 않은** 오류가 섞입니다.

        `--no-fail-fast`를 함께 붙이기를 권합니다. `cargo test`는 기본이 fail-fast라 실패한 타깃
        뒤의 타깃을 **한 건도 실행하지 않습니다** — `#49` 때 24번째에서 멈춰 25번째
        (`audit_integration`)가 통째로 보이지 않았습니다. 이것은 §4.3 (3)의 조용한 skip과 달리
        개수도 소요 시간도 아예 출력되지 않는 형태의 누락입니다.
3.  **Flaky 테스트 방치 금지**:
    테스트 도중 간헐적으로 깨지는 테스트가 식별되면 환경적 영향(OS 소켓 점유 등)인지, 락(Lock) 경쟁 등의 런타임 버그인지 명확히 규명하고 가이드라인에 기록해야 합니다.

    *   **기록 1 — `fleet-mcp`의 `cross_client` (2026-08-26, 환경적 영향)**: `cargo build -p fleet-cli`
        직후의 `cargo test --workspace`에서 14건 중 1건이 `RESPONSE_TIMEOUT`(15초)을 넘겨 "no
        initialize response"로 실패했습니다(`13 passed; 1 failed ... finished in 17.08s`). 단독
        재실행은 14건 0.66초로 통과했습니다. **원인은 macOS의 첫 exec 비용입니다** — 디버그
        `fleet` 바이너리가 116MB라 코드서명 검증과 페이지 인이 첫 실행에만 붙습니다. 실측: 갓
        복사한 바이너리의 첫 `serve` 응답 1.35~2.34초, 두 번째 0.03~0.04초(35~78배). 이 비용은
        **프로세스의 첫 spawn 하나만** 지불하므로 14건 중 정확히 1건, 그것도 알파벳 순 첫 테스트가
        실패한 관측과 일치합니다. **배제한 가설도 실측으로 남깁니다**: 콜드 DB의 26개 마이그레이션은
        첫 응답 0.19초(웜 0.02초)로 크기가 맞지 않고, 커넥션 고갈도 아닙니다 — 서버 20개 동시 기동
        (잠재 200 연결, 서버측 `max_connections=100`)에서 전부 ~0.10초였고 풀이 lazy라 실제 연결은
        0이었습니다. 대응은 `spawn_server()`의 `std::sync::Once` 워밍업(`fleet --version`, DB 미접속)
        으로 그 비용을 타이머 **밖**에서 치르는 것입니다(A/B 실측: 워밍업 없음 1.35초 → 워밍업 후
        0.04초). CI도 두 잡 모두 `cargo build -p fleet-cli` 직후 `cargo test`를 돌리므로 **같은
        노출을 갖습니다** — 워크플로 변경 없이 테스트 파일 수정만으로 양쪽에 적용됩니다.
        **검증 한계**: 유휴 시 ~1.4초가 부하에서 15초를 넘기는 증폭 자체는 재현하지 못했고(12개
        테스트 바이너리 동시 실행이라는 정황에서 추론), 15초 실패는 한 번 관측된 뒤 재현되지
        않았습니다. 즉 이 수정은 **재현 후 수정이 아니라 기전(mechanism) 실측으로 정당화**됩니다.

        **여기서 "타이머"는 응답당 `RESPONSE_TIMEOUT`(15초)이지 suite 총 소요 시간이 아닙니다.**
        워밍업은 비용을 첫 응답 대기 밖으로 옮길 뿐 없애지 않으므로, 갓 빌드된 바이너리로
        시작하면 **suite 자체는 여전히 8~12초가 걸립니다**(2026-08-27 실측: `rm target/debug/fleet`
        후 재빌드 직후 7.84초 → 바로 다음 실행 0.74초, 10.6배). 이 숫자를 보고 워밍업이 고장났다고
        읽지 마십시오 — 세 번 모두 14/14 통과했고, 그것이 워밍업이 제 역할을 했다는 증거입니다.
        **판정은 소요 시간이 아니라 통과 여부입니다.** 반대로 §4.3의 조용한 skip 탐지에서는 소요
        시간이 판정이었습니다 — 같은 숫자가 무엇을 말하는지는 무엇을 의심하고 있느냐에 달립니다.

4.  **플랫폼 의존 단정 금지 — 메모리 값과 왕복 값을 직접 맞대지 않습니다**:
    Postgres `timestamptz`는 **마이크로초 해상도**입니다. 반면 `Utc::now()`는 나노초를 줍니다.
    따라서 `assert_eq!(stored.some_time, in_memory.some_time)`은 나노초 잔여가 있는 플랫폼에서만
    깨집니다 — **macOS의 `CLOCK_REALTIME`은 잔여가 항상 0이라 절단이 일어나지 않고, 진짜
    나노초를 주는 Linux CI에서만 실패합니다**(2026-08-29에 `worker_incarnation.rs`가 이렇게
    깨졌습니다: `left ...212633Z` vs `right ...212633416Z`, 로컬은 양쪽 피처 세트에서 71 suite
    전부 초록이었습니다).

    *   양쪽이 **모두 왕복한** 값끼리의 비교는 안전합니다 — 절단이 대칭이기 때문입니다.
        해상도를 낮춰야 하는 자리는 **메모리 값 대 저장된 값**뿐이며, 다른 곳까지 낮추면
        단정만 약해집니다.
    *   비교는 양변을 `timestamp_micros()`로 내려서 합니다.
    *   **더 중요한 것은 재현 가능하게 만드는 것입니다.** 시각을 만드는 헬퍼에서 나노초 잔여를
        **일부러** 심으면(`w.registered_at += chrono::Duration::nanoseconds(416);`) 테스트가 어느
        플랫폼에서든 같은 경로를 밟습니다. 이 줄이 없으면 그 파일은 "Linux CI에서만 깨지는
        테스트"가 되고, §4.3의 로컬 게이트로는 **원리적으로** 잡을 수 없습니다.
    *   이것은 §4.3이 기록해 온 "게이트와 CI가 다르면 로컬 판정은 CI 판정이 아니다"의 네 번째
        얼굴입니다. 앞의 셋(목록 누락·툴체인 드리프트·런타임 전제 누락)과 달리 **명령을 더
        붙여서 막을 수 없습니다** — 커널의 시계 해상도가 다르기 때문입니다. 막는 자리는
        게이트가 아니라 테스트입니다.


---

## 4. CI 및 피처 게이트(Feature Gate) 검증 기준

GitHub Actions CI 환경에서 조건부 컴파일 및 코드 검사가 깨지지 않도록 하기 위해 다음 규칙을 필수로 이행합니다.

1.  **조건부 임포트와 네임 리졸브 방지**:
    *   특정 피처(예: `acp`, `mtls` 등)가 꺼진 최소 기능 빌드(`--no-default-features`)를 돌릴 때, 해당 피처 하위에만 존재하는 타깃 타입(예: `AcpTransport`, `ClientTlsConfig`)을 코드 상에서 직접 지칭(예: `fleet_transport::AcpTransport`)하여 Name resolution 오류가 유발되지 않도록 주의해야 합니다.
    *   관련 외부 크레이트 바인딩은 상단 임포트 시 `#[cfg(feature = "...")]` 로 가드하고, 코드 내에서는 네임스페이스 접두사 없이 직접 가져온 타입을 바인딩하여 컴파일러 파싱 영역에서 안전하게 제거되도록 구현해야 합니다.
2.  **Clippy 경고 무결성 준수**:
    *   코드 빌드 시 clippy 경고가 발생하면 CI 파이프라인이 즉시 실패합니다.
    *   특히 `#[cfg(test)] mod tests` 모듈 뒤에 임의의 공개/비공개 헬퍼 함수를 정의하는 것은 `clippy::items_after_test_module` 경고를 유발하므로, 테스트 모듈 뒤에는 어떠한 일반 구현체도 배치하지 말고 항상 파일의 최하단에 테스트 모듈을 두도록 코드를 구조화합니다.
3.  **원격 Push 전 로컬 자가 진단**:
    *   수정 사항을 `origin` 원격지로 푸시하기 전에, 에이전트와 개발자는 로컬 터미널에서 다음 검증을 반드시 통과시켜야 합니다. **CI(`.github/workflows/ci.yml`)가 실행하는 것과 동일한 형태**로 적어 둔 것이므로 축약하지 말고 그대로 실행합니다:
        ```bash
        # 0-a. 컴파일러가 CI와 같은지 먼저 확인 — rust-toolchain.toml의 고정 버전과 일치해야 함
        rustc --version

        # 0-b. CI의 workflow-level env(ci.yml의 `env: RUSTFLAGS`)와 같은 shape를 재현
        export RUSTFLAGS="-D warnings"

        # 1. 포맷 검사 — CI의 첫 단계이며, 실패하면 이후 단계는 실행조차 되지 않음
        cargo fmt --all -- --check

        # 2. 기본 피처 세트(acp+mtls)에서의 Clippy 경고 유무 확인
        cargo clippy --workspace --features "acp mtls" --all-targets -- -D warnings

        # 3. 최소 기능 빌드에서의 Clippy 경고 유무 확인
        cargo clippy --workspace --no-default-features --all-targets -- -D warnings
        ```
    *   **`-D warnings`를 빼지 않습니다.** 이 플래그가 없으면 clippy는 경고가 있어도 종료 코드 0을 반환하므로, "경고 유무를 확인했다"는 판단이 성립하지 않습니다. 마찬가지로 3번을 `cargo check`로 대체하면 컴파일 오류만 잡히고 lint는 통과 여부를 알 수 없습니다.
    *   **판정을 파이프 뒤에서 읽지 않습니다.** `cargo clippy ... | tail -5` 다음의 `$?`는 clippy가
        아니라 `tail`의 종료 코드이며, 항상 0입니다. zsh의 `PIPESTATUS`는 1-기반이라 `${PIPESTATUS[0]}`도
        빈 문자열을 줍니다(2026-08-26 실측, 두 방식 모두 게이트를 통과한 것처럼 보였습니다). 출력의
        `Finished` 줄이 보인다는 것으로 성공을 **추론**하는 것도 판정을 읽은 것이 아닙니다. 파이프 없이
        돌리고 상태를 직접 찍습니다:
        ```bash
        cargo clippy --workspace --no-default-features --all-targets -- -D warnings > /dev/null; echo "exit=$?"
        ```
        이것은 위 bullet과 같은 결함의 다른 얼굴입니다 — 거기서는 플래그가 없어서, 여기서는 플래그가
        있어도 **결과를 읽지 않아서** "확인했다"가 성립하지 않습니다.

        **이 규칙은 clippy 전용이 아닙니다.** 2026-08-27에 같은 결함이 `cargo test --workspace ... | tail -60`
        으로 재발했습니다. 종료 코드는 `tail`의 것이라 0이었고, 캡처된 60줄은 doc-test 꼬리뿐이라
        "10 suites / 1 passed"라는 말이 안 되는 요약이 나왔습니다. 규칙은 이미 있었지만 **예시가
        clippy로만 적혀 있어서** 명령이 바뀌자 적용 대상으로 읽히지 않았습니다. 판정을 읽어야 하는
        모든 게이트 명령에 걸립니다 — `cargo test`도, 아래 CI 조회도 포함입니다.

    *   **CI의 `conclusion`도 판정처럼 보이지만 판정이 아닐 수 있습니다.** run의 `conclusion`은 잡
        결과의 집계가 아니라 run 자체의 종료 상태입니다. 2026-08-26의 `ddd2b85`는 GitHub Actions
        장애로 네 잡이 전부 `status: queued`에 `completedAt`이 제로 타임(`0001-01-01T00:00:00Z`)인
        채로 run만 `failure`로 닫혔습니다 — **한 줄도 실행되지 않았는데 `main`에 빨간 X가 남습니다.**
        `conclusion`만 읽으면 코드 실패로 오독하고 없는 버그를 쫓게 됩니다. 잡 상태를 함께 읽습니다:
        ```bash
        gh run view <id> --json conclusion,jobs --jq '{conclusion, jobs:[.jobs[]|{name,status,conclusion}]}'
        ```
        `status`가 `completed`가 아닌 잡이 있으면 그 run은 코드에 대해 **아무것도 말하지 않습니다**.
        §3.2의 조용한 skip(`14 passed`를 `finished in 0.00s`가 뒤집는 것)과 같은 형태입니다 —
        **판정처럼 보이는 값 옆의 메타데이터가 그 값을 무효화합니다.**
    *   **게이트를 돌린 사실과 그 게이트가 지금 트리를 가리키는 것은 다릅니다.** 2026-08-28에
        `#49` 후속 수정을 커밋하기 직전, 기록된 게이트 로그는 다섯 줄 전부 `exit=0`이었지만
        그 로그의 mtime이 **13:34**이고 고친 소스들의 mtime은 **13:51~13:57**이었습니다 — 초록은
        고치기 *전* 트리에 대한 판정이었고, 그대로 커밋했다면 실제로 검증된 적 없는 코드가
        "게이트 통과"로 기록될 뻔했습니다. 위 두 bullet과 같은 결함의 세 번째 얼굴입니다:
        거기서는 플래그가 없어서, 그다음은 파이프 때문에 결과를 못 읽어서, 여기서는 결과는
        읽었지만 **대상이 이미 바뀌어서** "확인했다"가 성립하지 않습니다. 커밋 직전에 확인합니다:
        ```bash
        ls -lt $(git diff --name-only) | head -3   # 소스 최신 mtime
        ls -l  <게이트 로그>                        # 게이트가 그보다 나중인지
        ```
        게이트 로그가 더 이르면 게이트를 **다시 돌립니다**. 판정을 신선도로 무효화하는 이
        형태는 §3.2의 소요 시간, 아래 CI의 잡 `status`와 같은 계열입니다.
    *   **0-a와 0-b는 장식이 아니라 게이트의 일부입니다.** `RUSTFLAGS`는 `-- -D warnings`와 겹치지 않습니다 — 후자는 clippy lint에만 걸리지만 전자는 rustc 경고 전체에 걸리며, CI는 이를 `test`·`coverage` 잡에도 적용합니다. 툴체인이 다르면 존재하는 lint 집합 자체가 달라지므로, 두 줄 중 하나라도 빠지면 나머지 세 명령이 모두 통과해도 CI 통과를 예측하지 못합니다.
    *   이 목록이 CI보다 약해서 실패한 사례가 2026-08-25 하루에 두 번 확인됐습니다. (1) `cargo fmt --all -- --check`가 목록에서 빠져 있던 동안 33개 파일에 112건의 포맷 위반이 누적되어 `main`의 CI가 첫 단계에서 실패하는 상태로 방치됐습니다. (2) 툴체인이 `stable` 부동이라 로컬 1.97.1 / CI 1.98.0으로 갈렸고, 1.98.0이 도입한 clippy lint 3종이 CI에서만 나타나 8회 연속 실패했는데 로컬 게이트는 그동안 계속 초록이었습니다. **게이트 목록이 CI보다 약하면 드리프트는 반드시 재발합니다.** (1)은 목록이 짧아서, (2)는 명령이 같아도 실행 환경이 달라서 생긴 것으로, 드러난 자리만 다를 뿐 같은 결함입니다.
    *   2026-08-26에 세 번째 사례가 확인됐습니다. (3) `cargo test`가 만들지 않는 `target/debug/fleet`에
        `cross_client` 테스트가 의존하는데, 이 전제가 어느 게이트에도 적혀 있지 않았습니다. 캐시가
        복원된 잡은 **낡은** 바이너리로 실패했고, 캐시가 없던 잡은 바이너리 **부재**로 14건 전부를
        조용히 건너뛴 채 `14 passed`를 보고했습니다. 후자는 통과 개수만 보면 실행과 구분되지 않지만
        `finished in 0.00s`(로컬 실제 실행은 5.12s)가 그것을 가릅니다. **개수가 아니라 소요 시간이
        조용한 skip을 드러냅니다.** 대응은 §3.2의 build 단계와 하네스의 panic 두 겹입니다.
        단 그 panic은 `spawn_server()`에서 `database_url()?`의 **뒤**에 있습니다 — `DATABASE_URL` 없이
        돌리면 조기 반환이 먼저 걸려 두 겹이 **동시에** 무력해지고, 바이너리가 없어도 다시 `0.01s`에
        `14 passed`가 됩니다(2026-08-26 실측). 방어의 강도가 아니라 **순서**가 도달 가능성을 정하므로,
        위 bullet 2의 DB 게이트를 조건부로 읽지 말아야 할 이유가 여기에 하나 더 있습니다.

---

## 5. 문서 정본성 및 재작성 규약

문서는 코드와 동등한 프로젝트 자산이다. 문서를 생성하거나 크게 수정할 때는 다음 정본을 따른다.

- [문서 관리 정책](./docs/governance/documentation-policy.md): 도메인, 정본 관계, 메타데이터와 부기 원칙
- [문서 재작성 가이드](./docs/governance/documentation-rewrite-guide.md): 대규모 재작성·이동·폐기의 절차와 완료 게이트
- [Reviews](./docs/reviews/README.md): 비교·감사·논의 근거의 보존 위치

에이전트는 문서 작업 전에 대상 도메인 진입점에서 책임과 정본을 확인한다. 현재 구현을 언급하면
코드·테스트·설정과 대조하고, 변경 뒤에는 상대 링크·메타데이터·필요한 색인과 로그를 검증한다.
정책 전문이나 다이어그램 규칙을 이 파일에 복사하지 않는다.

단순 오탈자처럼 책임·정본·탐색 구조를 바꾸지 않는 수정에는 재작성 가이드 전체 절차를 요구하지
않는다. Git 커밋은 사용자가 요청했거나 작업 흐름에서 권한이 명시된 경우에만 수행한다.
